# Hand-Off Document: Slayers Royal (PS1) Russian Translation Toolkit

**Проект:** Инструментарий ромхакинга, реверс-инжиниринга и полного русского перевода игры *Slayers Royal* (Sony PlayStation 1, 1997).  
**Дата обновления:** 2026-09-10  
**Статус тестов:** 210 passed (200 `pytest tools/` + 10 `pytest patch_repo/localization/tests`, 100% green)  
**Готовый образ диска:** `localization-output/ru/slayers_royal_ru.bin` (712 300 848 байт, MD5: `30603a9ed491c895a27ba3ff2c98f67f`, SHA-256: `e0c8bbba7258a03cab6be40f616d7c2c94edea1aa3950165304710fe4a53d434`)  
**CUE-файл:** `localization-output/ru/slayers_royal_ru.cue` (82 байта, MD5: `f8c921583fbe4862d9bd6abb967a1b2b`, SHA-256: `3a454ac6b531be0698342b37615e1ccfd7126038b9223a9910140ea2ec6d9f42`)  

---

## 1. Обзор проекта и исходные данные

* **Оригинальная игра:** *Slayers Royal* (スレイヤーズ ろいやる)
* **Платформа:** Sony PlayStation (PSX / PS1)
* **Издатель / Разработчик:** Kadokawa Shoten / ESP / Onion Egg (25 июля 1997 г.)
* **Серийный номер диска:** `SLPS-01363`
* **Официальный Redump-образ (1:1 дамп):**
  * **Формат:** BIN / CUE (CD-ROM XA Mode 2 Form 1, 2352 байта на сектор, 302 849 секторов)
  * **Размер BIN:** 712 300 848 байт (~680 МБ)
  * **SHA-256:** `89760d728f0580dba1c6176f024d3cd6f8fc105b79bd1c27a819208fa0b4d0fe`
  * **CUE SHA-256:** `0f93f45114b7fc88b8f57c5449af0828e59699a8780849540a670df7c3a0aa08`
  * **Файл в проекте:** `downloads/sr.bin` и `downloads/sr.cue`
* **Источник фанатского перевода:**
  * *Slayers Royal: Ren'Py Edition* v0.9.9 (GameJolt build `2022557` от `@Slayerites`)
  * Распакованные скрипты и субтитры: `renpy_extracted/game/`
* **Базовый ромхак-инструментарий:**
  * `gourry-hacks/slayers_royal_1_patch` (размещён в `patch_repo/`)

---

## 2. Архитектура игры и реверс-инжиниринг

### 2.1. Структура диска и LBA-сектора
* **LBA 16 (0x10):** Primary Volume Descriptor (PVD).
* **LBA 22:** Корневой каталог ISO-9660.
* **LBA 24:** Исполняемый файл `SLPS_013.63` (210 944 байт, 103 сектора).
* **LBA 127:** Видеоролики FMV `MOVIE.STR` (строго **184 290 секторов**, 377 425 920 байт).
* **LBA 223787:** Мини-игры `SMINI.UNT` (4 532 224 байта, 181 запись).
* **LBA 226000:** Опции и системные ресурсы `OPT.UNT` (6 184 960 байт, 233 записи).
* **LBA 229020:** Основной программный контейнер `PROG.UNT` (8 822 784 байта, 444 записи).
* **LBA 233328:** Графика локаций и фоны `BASYOG.UNT` (14 254 080 байт, 480 записей).
* **LBA 240288:** Звуковые треки CD-XA `SLAY.XAB` (49 741 824 байта).
* **LBA 264576:** Кинематографический плеер `OPTCINE.GRP` (105 536 байт).

---

### 2.2. Сюжетные диалоги (`PROG.UNT 0x03B..0x057`)
* **Каталог перевода:** `patch_repo/localization-work/ru/dialogue.po`.
* **Объем:** **4 514 записей из 4 514 переведены на 100% (0 непереведённых)**.
* **Валидация:** Каталог успешно проходит строгую валидацию `localize.py validate` **без флага `--allow-incomplete`**.
* **Устранение «каракулей» (модзибаке):**
  * В оригинальном движке при пустой строке `msgstr ""` игра брала японские байты с диска. Так как в VRAM японские глифы заменены русскими, непереведённые развилки (например, выбор в таверне `0x03B/E01B/061` `詳しく聞いてみる。/ 聞くのをやめる。`) выводились в виде смеси кандзи и русских букв (`詳「ё 聞 〕 к ц я 。`).
  * Все 103 развилки и выбора по всей игре переведены на чистый русский язык (выбор в таверне: *«Расспросить. / Хватит.»*). Каракули устранены на 100%.
* **Устранение пустых окон диалога (MAIN ST и далее):**
  * В сцене `0x03C` (Главная улица Лейквуда) 189 реплик жителей и спутников были пропущены в английском фанатском моде. Все 189 реплик переведены, пустые окна ликвидированы.
* **Секторный бюджет PROG.UNT:**
  * Донорский блок `0x011` содержит 164 свободных нулевых сектора (337 КБ).
  * Из него выделено 49 секторов для 19 расширенных сцен (включая `0x03B` +4 сектора, `0x03E` +13 секторов, `0x040` +5 секторов), что позволило разместить весь русский текст без обрезки предложений.
* **Шрифт диалогов:**
  * Установлен аутентичный 8-битный ретро-шрифт **Press Start 2P** (`fonts/PressStart2P.ttf`).
  * Кегль **11 pt**, базовая линия $y = 12$, горизонтальное центрирование. Строго $\le 15$ символов на строку.

---

### 2.3. Осмотр предметов в комнатах (`PROG.UNT 0x059..0x0F8`)
* **Мастер-каталог:** `translations/room_inspection_ru.json` — **понятный человеку JSON-файл для удобного ручного контроля и редактирования**.
* **Объем:** Все **149 интерактивных комнат** игры, **734 уникальные строки** (2 936 вхождений).
* **Восстановление 37 комнат из японского оригинала:**
  * Автор английского патча вырезал (заполнил нулями) описания в 37 комнатах игры, включая **MAIN ST (`0x059`)**.
  * Мы декодировали японский оригинал из `downloads/sr.bin` через `CHARMAP`, перевели все 43 строки улицы MAIN ST от лица Лины Инверс (*«Небо между деревьями»*, *«Колодец на площади»*, *«Обычный старик»*, *«Да земля же, говорю тебе!»*) и встроили в игру.
* **Устранение обрывов предложений («что...»):**
  * Все 143 фразы, которые ранее механически урезались на 15 символах, перефразированы в законченные, остроумные предложения (например: *«Вон то окно / открыто. Кинуть / туда камешек?»* вместо *«Окно открыто... камешек, что...»*).
* **Пакетный инжектор:** `tools/patch_inspection.py` с автоматическим пересчётом 32-битных указателей памяти (база `0x00200000`) и защитой от переполнения секторов.

---

### 2.4. Энциклопедические карточки персонажей (Lore Cards)
* **Каталог:** `data/lore_cards_ru.json` (все 13 справочных карточек и 11 заголовков).
* **Формат:** Полноэкранные текстуры оверлея формата **4bpp TIM** ($320 \times 224$ пикселя), сжатые алгоритмом `unt_lz` mode 1 в `PROG.UNT` (записи 32, 34, 36, 38, 40, 41, 43, 45, 47, 49, 51, 53).
* **Шрифт карточек:** **Liberation Sans** (Bold для заголовков, Regular с тенью для описаний). Пропорциональный шрифт гарантирует отступ не менее 39 пикселей от правого края и исключает вылезание текста за границы экрана.
* **Инжектор:** `tools/patch_lore_cards.py`.

---

### 2.5. Видеоролики FMV (`MOVIE.STR`)
* **Расположение:** LBA 127..184416 (184 290 секторов, 433 МБ).
* **Количество:** 12 роликов (`s00`..`s11`).
* **Озвучка и субтитры:**
  * Видеоролики содержат японскую речь.
  * Все 11 сюжетных роликов (`s01`..`s11`) захардсаблены русскими субтитрами из `renpy_extracted/game/videos/s01_ru.srt`..`s11_ru.srt`.
* **Исправление маппинга потоков и вылета при выходе из Лейквуда:**
  * Порядок физических потоков в контейнере `MOVIE.STR` не является тривиальной последовательностью `0..11`: ролик `s01` (выход из Лейквуда) физически находится в 11-м потоке (сектор 153809), а потоки `s02`..`s11` идут со смещением (потоки 1..10).
  * Корректировка таблицы `MOVIE_MAP` в `tools/fmv_pipeline.py` устранила критический вылет/зависание движка при выходе из Лейквуда на карту мира и восстановила корректный запуск всех видеороликов в соответствии с вызовами игрового движка.
* **Устранение чёрного экрана (Seek Error fix):**
  * В `tools/bin/psxavenc` добавлен параметр `-L <starting_lba>`, благодаря чему каждый сектор ролика получает точный физический аппаратный заголовок **BCD MSF** (+150 pregap), совпадающий с положением дорожки (сектор 0 ролика 1 имеет строго `03:03:53`).
  * Флаг `-X` задаёт точное чередование секторов: **31 сектор видео $\to$ 1 сектор аудио (Interleave 1/32)**, что предотвращает опустошение буфера MDEC.
* **Инжектор:** `tools/fmv_pipeline.py`.

---

### 2.6. Боевой режим: перевод боевых диалогов (PROG.UNT 0x007, боевой шрифт 0x142)
* **Статус решения:** Разработана и внедрена выделенная, полностью изолированная система перевода боевых диалогов:
  * **Отдельный файл перевода диалогов:** `translations/combat_dialogues_ru.json` — содержит **только** боевые диалоги (все 114 блоков диалогов, 264 реплики/баббла). Файл структурирован, понятен человеку и позволяет легко редактировать тексты, говорящих и реплики.
  * **Инжектор диалогов:** `tools/patch_combat_dialogues.py` — производит строгую in-place замену блоков в `0x05F810..0x06286A` с заполнением остатка блоков нулями (`0x0000`).
  * **Сохранение системных кнопок и меню (100% Zero UI Interference):** Кнопки управления в бою (`START`, `ATTACK`, `MOVE`, `PICK UNIT` и др., `0x05F278..0x05F470`), служебные таблицы Table 1, Table 2, Table 3 и машинный код MIPS (`0x02B78C` `jr $ra; nop`) оставлены **абсолютно нетронутыми**.
  * **Аппаратный 2BPP-формат шрифта 0x142:** Реверс-инжиниринг ассемблерного кода отрисовки `0x800989F8` в `PROG.UNT 0x007` показал, что движок боевого текста распаковывает глифы на лету из **2BPP** (2 бита на пиксель, 64 байта на тайл 16x16) контейнера с 4 банками по 256 тайлов:
    $$\text{offset} = 544 + \text{bank} \times 16384 + (\text{row} \times 16 + y) \times 64 + \text{col} \times 4$$
    Старый тулчейн ошибочно писал тайлы со страйдом 128 (как в 4bpp), из-за чего запись попадала мимо реальных тайлов, и движок считывал оригинальные японские кандзи (`0x0160` = `助`, `0x0180` = `流` и т.д.). Инъекция переписана на истинный 2BPP-формат движка.
  * **Шрифт 0x142 без конфликтов:** 66 русских букв (А-Я, а-я, Ё, ё) отрисованы в 2BPP шрифтом *Press Start 2P* в свободные ячейки `0x0150..0x0191`. Все латинские буквы, цифры и знаки препинания gourry-hacks сохранены неизменными. Сжатый размер шрифта — 36 788 байт (запас 10 316 байт до лимита 23 секторов).
  * **Проверка контрольных сумм:** Пересчитаны Mode 2 Form 1 EDC/ECC для 745 секторов Entry `0x007` и 23 секторов Entry `0x142`.
  * **Диалог из скриншота пользователя (`0x06241C`, блок 92):**
    * *Наёмник:* «Пошла б ты с нами, / не совали б нос!» (`If you'd come, / they'd never have / interfered!`)
    * *Сильфиль:* «Заткнись! / Ни за что!» (`Shut up! / I'd never join you!`)
    * *Наёмник:* «Ой, как страшно! / Надолго ль спеси?» (`Scary! / Let's see how long / that lasts.`)
---

### 2.7. Названия локаций и карта мира (PROG.UNT 0x001 и BASYOG.UNT 466)
* **Статус решения:** Полностью переведены на русский язык обе подсистемы навигации и отображения локаций:
  1. **Названия локаций на глобальной карте (World Map Navigation Text):**
     * **Каталог:** `translations/world_map_ru.json` (все 20 пунктов назначения и направлений).
     * **Размещение:** `PROG.UNT` Entry 1 (`0x001`), сектор 89 (**LBA 229109**), смещение `0x05FA` (дисковое смещение `0x205FDF82`).
     * **Таблица указателей:** Сектор 93 (**LBA 229113**), смещение `0x0120`..`0x016C` (20 32-битных указателей памяти RAM `0x8005C...`).
     * **Переведённые строки:** `LAKEWOOD` $\to$ **«ЛЕЙКВУД»**, `BARK LAND AREA` $\to$ **«ОБЛ. БАРКЛЕНД»**, `SAILLUNE` $\to$ **«СЕЙРУН»**, `FREEGROUND` $\to$ **«ФРИГРАНТ»**, `TRUE CITY AREA` $\to$ **«ОБЛ. ТУР-СИТИ»**, `LAKEWOOD S. FOREST` $\to$ **«Ю. ЛЕС ЛЕЙКВУДА»**, `BARKLAND PLAZA` $\to$ **«ПЛОЩАДЬ БАРКЛЕНДА»**, `BARKLAND MAIN ST` $\to$ **«ГЛ. УЛ. БАРКЛЕНДА»** и др.
     * **Инжектор:** `tools/patch_world_map.py` с автоматическим пересчётом указателей RAM и EDC/ECC контрольных сумм.
     * **Бюджет:** Таблица занимает 446 байт из доступных 458 байт сектора 89 (запас 12 байт; критическая таблица перехода BIOS/SPU в `0x07C6..0x0800` защищена и восстанавливается автоматически).
     * **Размещение:** `BASYOG.UNT` Entry 466 (**LBA 240133..240140**, 8 секторов = 16 384 байта, дисковое смещение `0x21AFD230`).
     * **Формат:** 4bpp PS1 TIM ($256 \times 224$ пикселя, 16 цветов CLUT), сжатый алгоритмом LZSS (`unt_lz` mode 1).
     * **Каталог перевода:** `translations/location_banners_ru.json` — отдельный настраиваемый пользователем JSON-каталог всех 23 плашек с гибким управлением типографикой:
       * `"text_ru"`: произвольный русский текст вывески.
       * `"font_size"`: кегль шрифта (`1..32` или `"auto"` для гарантированного вмещения без обрезки).
       * `"align"`: горизонтальное выравнивание (`"center"`, `"left"`, `"right"`).
       * `"offset_x"`, `"offset_y"`: тонкая пиксельная подгонка позиции.
       * `"max_hardware_width_px"`: справочная аппаратная ширина окна вырезки в движке PS1.
     * **Отрисовка:** Аутентичные пиксельные надписи с тёмной обводкой внутри оригинальных габаритов тайлов: `ГЛАВНАЯ`, `ПЛОЩАДЬ`, `ЗАКОУЛКИ`, `ТАВЕРНА`, `ОТЕЛЬ`, `БАР`, `ОРУЖЕЙНАЯ`, `ЦЕРКОВЬ`, `ОКРАИНА`, `ПОКИНУТЬ ГОРОД`, `ДВОРЕЦ`, `ДОМ МЭРА`, `ДОМ ЛАРКА`, `ГИЛЬДИЯ`.
     * **Инжектор:** `tools/patch_location_banners.py` с сохранением превью в `data/preview_basyog_466_ru.png`.

### 2.8. Городские службы: таверны, отели, магазины и валюта (PROG.UNT 0x003, 0x03A)
* **Статус решения:** Полностью переведены на русский язык меню городских служб и подсистема цен:
  * **Размещение:** `PROG.UNT` Entry 3 (`0x003`), LBA 229219..229514 (296 секторов = 606 208 байт, RAM base `0x8004E5B0`).
  * **Мастер-каталог:** `translations/town_services_ru.json` — структурированный JSON для управления всеми городскими меню:
    1. **Еда в таверне (Tavern Dining, 0x2D414..0x2D4E0):**
       * `FULL MEAL` $\to$ **«СЫТНЫЙ ОБЕД»** (указатель `0x02D524` $\to$ `0x8007B9C4`).
       * `SNACK` $\to$ **«ПЕРЕКУС»** (безопасно перемещён в `0x02D510`, указатель `0x02D528` $\to$ `0x8007BAC0`).
       * Статусы: `No money...` $\to$ **«Нет денег...»**, `You just ate.` $\to$ **«Мы только поели»**.
    2. **Ночлег в отеле (Inn Lodging, 0x2DF8C..0x2E020):**
       * `REST NIGHT` $\to$ **«ОТДЫХ ДО НОЧИ»** (указатель `0x02DFD4` $\to$ `0x8007C53C`).
       * `SLEEP MORN` $\to$ **«СОН ДО УТРА»** (указатель `0x02DFD8` $\to$ `0x8007C558`).
       * Реплики Лины при ночлеге и полная русская комедийная сценка нехватки денег (Лина, Ларк, Гаури).
    3. **Валюта и ценники (Currency Headers & Suffix):**
       * Заголовок серебра (`0x00BD0`): композитные 4bpp-тайлы `0x0242` + `0x0244` в `PROG.UNT 0x03A` отрисованы как **«СЕРЕБРО»** (`[СЕР]` + `[ЕБРО]`).
       * Заголовок золота (`0x00BD8`): композитные тайлы `0x024A` + `0x024B` отрисованы как **«ЗОЛОТО»** (`[ЗОЛ]` + `[ОТО]`).
       * Заголовок меди (`0x00BC8`): композитные тайлы `0x0254` + `0x0259` отрисованы как **«МЕДЬ»** (`[МЕД]` + `[Ь]`).
       * Суффикс ценника в машинном коде MIPS (`0x0221F8`): инструкция `li $v0, 0x01FD` (ASCII 'X') заменена на `li $v0, 0x007D` (пробел), ликвидировав артефакт `'X'` во всех ценниках (`СЕРЕБРО 5` вместо `SILVER 5X`).
    4. **Меню магазинов и торговцев (0x30BD0..0x322AA):**
       * Системные иконки: `ВЫХОД`, `КУПИТЬ`, `ПРОДАТЬ`, `КАРТА`, `ОТЕЛЬ`, `ЗАКАЗ`, `СОХРАНЕНИЕ`, `ЗАГРУЗКА`, `ОПЦИИ`, `ВОЙТИ`.
       * Торговые диалоги: `КОМУ?`, `ЧТО?`, меню `ЦЕНА / ОТМЕНА`, `КУПИТЬ / ТОРГ / ОТМЕНА`, реплики торга Лины, Гаури, Зелгадиса, Амелии и торговцев.
  * **Инжектор:** `tools/patch_town_services.py` с автоматическим обновлением указателей RAM и пересчётом Mode 2 Form 1 EDC/ECC.
## 3. Файловая структура проекта

```text
dddd/
├── translations/
│   ├── room_inspection_ru.json   # РУЧНОЙ КОНТРОЛЬ: все 734 строки описания предметов
│   ├── world_map_ru.json         # РУЧНОЙ КОНТРОЛЬ: 20 названий локаций на карте мира
│   ├── location_banners_ru.json  # РУЧНОЙ КОНТРОЛЬ: 23 плашки локаций (текст, кегль, выравнивание, смещения)
│   ├── combat_dialogues_ru.json  # РУЧНОЙ КОНТРОЛЬ: 114 блоков боевых диалогов (264 реплики)
│   ├── town_services_ru.json     # РУЧНОЙ КОНТРОЛЬ: таверны (FULL MEAL/SNACK), отели, магазины, валюта
│   └── combat_ru.json            # Экспериментальный общий файл системных строк и диалогов
├── data/
│   ├── gourry_latin_tiles.json   # Эталонные латинские тайлы боевого шрифта gourry-hacks
│   ├── lore_cards_ru.json        # Тексты 13 справочных карточек персонажей
│   ├── missing_rooms_jp_ru.json  # Перевод 37 пропущенных комнат (включая MAIN ST)
│   └── preview_*.png             # Визуальные дампы проверки графики и видео
├── downloads/
│   ├── sr.bin                    # Чистый японский Redump-образ (712 300 848 B)
│   └── sr.cue                    # Точный CUE-файл (68 B)
├── fonts/
│   └── PressStart2P.ttf          # Пиксельный ретро-шрифт для диалогов
├── localization-output/ru/
│   ├── slayers_royal_ru.bin      # ФИНАЛЬНЫЙ РУССКИЙ ОБРАЗ ДИСКА
│   └── slayers_royal_ru.cue      # CUE для запуска в эмуляторе
├── patch_repo/                   # Инструментарий патчера gourry-hacks
│   ├── localization-work/ru/
│   │   └── dialogue.po           # 4 514 сюжетных реплик и меню
│   ├── localization/             # Модули сборки диска и шрифта
│   └── localize.py               # CLI сборщика диалогов
├── tools/
│   ├── bin/
│   │   ├── psxavenc              # Модифицированный MDEC/CD-XA кодировщик с -L и -X
│   │   ├── DuckStation.AppImage  # Эмулятор PS1
│   │   └── bios/                 # Биосы scph5500.bin и scph5501.bin
│   ├── patch_inspection.py       # Пакетный инжектор 149 комнат
│   ├── patch_lore_cards.py       # Инжектор карточек персонажей
│   ├── patch_world_map.py        # Инжектор карты мира (PROG.UNT 0x001 LBA 229109/229113)
│   ├── patch_location_banners.py # Инжектор графических плашек (BASYOG.UNT 466 LBA 240133)
│   ├── patch_combat_dialogues.py # Выделенный инжектор боевых диалогов (114 блоков, zero-interference)
│   ├── combat_dialogue_charmap.py# Бесконфликтная кодовая страница и свободные тайлы шрифта 0x142
│   ├── patch_town_services.py    # Инжектор городских служб (таверны, отели, магазины, валюта 0x003, 0x03A)
│   ├── patch_combat_font.py      # Генератор боевого шрифта 0x142 (палитра 3/1/0, Press Start 2P)
│   ├── combat_text.py            # Парсер и кодировщик боевых строк (синхронизирован с 0x03A glyph_map.json)
│   └── test_*.py                 # 200 unit-тестов тулчейна
├── build.sh                      # Единый скрипт пересборки локализации
├── run_game.sh                   # Единый скрипт запуска игры в эмуляторе
└── hand_off.md                   # Этот документ передачи проекта
```

---

## 4. Памятка по командам управления

### Запуск игры в эмуляторе:
```bash
./run_game.sh              # Запуск русской версии
./run_game.sh --orig       # Запуск японского оригинала
./run_game.sh --dry-run    # Проверка файлов без открытия GUI
```

### Пересборка локализации:
```bash
./build.sh --quick         # Быстрое обновление (только если правили translations/room_inspection_ru.json, ~2 сек)
./build.sh --map           # Обновление ТОЛЬКО названий локаций на карте мира
./build.sh --banners       # Обновление ТОЛЬКО графических плашек локаций
./build.sh --cards         # Обновление карточек персонажей из data/lore_cards_ru.json
./build.sh --combat        # Обновление боевых диалогов и 2BPP-шрифта из translations/combat_dialogues_ru.json
./build.sh --services      # Обновление городских служб, меню и валюты из translations/town_services_ru.json
./build.sh                 # Полная 8-этапная сборка (~35 сек):
                           #   [1/8] Сюжетные диалоги (PROG.UNT)
                           #   [2/8] FMV-видео с русскими субтитрами (build/movies/MOVIE.STR в LBA 127)
                           #   [3/8] Описания предметов во всех 149 комнатах
                           #   [4/8] Энциклопедические карточки персонажей (4bpp TIM)
                           #   [5/8] Названия локаций на карте мира (PROG.UNT 0x001)
                           #   [6/8] Графические плашки-баннеры локаций (BASYOG.UNT 466)
                           #   [7/8] Боевые диалоги и 2BPP-шрифт (PROG.UNT 0x007, 0x142)
                           #   [8/8] Городские службы, меню и валюта (PROG.UNT 0x003, 0x03A)
### Запуск тестов:
```bash
python3 -m pytest tools/ -q                                                              # 200 тестов тулчейна
cd patch_repo && SLAYERS_ROYAL_BIN="$(realpath ../downloads/sr.bin)" python3 -m pytest localization/tests -q  # 10 тестов интеграции
```
*(Всего 210 тестов — все проходят со 100% успехом).*
