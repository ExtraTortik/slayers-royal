# Hand-Off Document: Slayers Royal (PS1) Russian Translation Toolkit

**Проект:** Инструментарий ромхакинга, реверс-инжиниринга и полного русского перевода игры *Slayers Royal* (Sony PlayStation 1, 1997).  
**Дата обновления:** 2026-09-16  
**Статус тестов:** 332 passed (100% green: 321 `pytest tools/` + 11 `pytest patch_repo/localization/tests`)  
**Готовый образ диска:** `localization-output/ru/slayers_royal_ru.bin` (712 300 848 байт, MD5: `57eed14f3220a31dae778a76f8382add`, SHA-256: `835a81f2823c04a4115230d523e4ffe9a13ddaabb1553b1851bbc76ced9bd1db`)  
**CUE-файл:** `localization-output/ru/slayers_royal_ru.cue` (82 байта, MD5: `f8c921583fbe4862d9bd6abb967a1b2b`, SHA-256: `3a454ac6b531be0698342b37615e1ccfd7126038b9223a9910140ea2ec6d9f42`)  

---

## 1. Обзор проекта и исходные данные

* **Оригинальная игра:** *Slayers Royal* (スレイヤーズ ろいやる)
* **Платформа:** Sony PlayStation (PSX / PS1)
* **Издатель / Разработчик:** Kadokawa Shoten / ESP / Onion Egg (25 июля 1997 г.)
* **Серийный номер диска:** `SLPS-01363`
* **Официальный Redump-образ (1:1 дамп):**
  * **Формат:** BIN / CUE (CD-ROM XA Mode 2 Form 1, 2352 байта на сектор, 302 849 секторов)
  * **Размер BIN:** 712 300 848 байт (~680 МБ)
  * **SHA-256:** `89760d728f0580dba1c6176f024d3cd6f8fc105b79bd1c27a819208fa0b4d0fe`
  * **CUE SHA-256:** `0f93f45114b7fc88b8f57c5449af0828e59699a8780849540a670df7c3a0aa08`
  * **Файл в проекте:** `downloads/sr.bin` и `downloads/sr.cue`
* **Источник фанатского перевода:**
  * *Slayers Royal: Ren'Py Edition* v0.9.9 (GameJolt build `2022557` от `@Slayerites`)
  * Распакованные скрипты и субтитры: `renpy_extracted/game/`
* **Базовый ромхак-инструментарий:**
  * `gourry-hacks/slayers_royal_1_patch` (размещён в `patch_repo/`)

---

## 2. Архитектура игры и реверс-инжиниринг

### 2.1. Структура диска и LBA-сектора
* **LBA 16 (0x10):** Primary Volume Descriptor (PVD).
* **LBA 22:** Корневой каталог ISO-9660.
* **LBA 24:** Исполняемый файл `SLPS_013.63` (210 944 байт, 103 сектора).
* **LBA 127:** Видеоролики FMV `MOVIE.STR` (строго **184 290 секторов**, 377 425 920 байт).
* **LBA 223787:** Мини-игры `SMINI.UNT` (4 532 224 байта, 181 запись).
* **LBA 226000:** Опции и системные ресурсы `OPT.UNT` (6 184 960 байт, 233 записи).
* **LBA 229020:** Основной программный контейнер `PROG.UNT` (8 822 784 байта, 444 записи).
* **LBA 233328:** Графика локаций и фоны `BASYOG.UNT` (14 254 080 байт, 480 записей).
* **LBA 240288:** Звуковые треки CD-XA `SLAY.XAB` (49 741 824 байта).
* **LBA 264576:** Кинематографический плеер `OPTCINE.GRP` (105 536 байт).

---

### 2.2. Сюжетные диалоги (`PROG.UNT 0x03B..0x057`)
* **Каталог перевода:** `patch_repo/localization-work/ru/dialogue.po` и мастер-каталог `translations/story_dialogues_ru.json`.
* **Объем:** **4 514 записей из 4 514 переведены на 100% (0 непереведённых)** во всех 30 сценах игры.
* **Валидация:** Каталог успешно проходит строгую валидацию `localize.py validate` **без флага `--allow-incomplete`** и аппаратную валидацию `python3 tools/sync_story_dialogues.py --validate`.
* **Устранение «каракулей» (модзибаке):**
  * В оригинальном движке при пустой строке `msgstr ""` игра брала японские байты с диска. Так как в VRAM японские глифы заменены русскими, непереведённые развилки (например, выбор в таверне `0x03B/E01B/061` `詳しく聞いてみる。/ 聞くのをやめる。`) выводились в виде смеси кандзи и русских букв (`詳「ё 聞 〕 к ц я 。`).
  * Все 103 развилки и выбора по всей игре переведены на чистый русский язык (выбор в таверне: *«Расспросить. / Хватит.»*). Каракули устранены на 100%.
* **Устранение пустых окон диалога (MAIN ST и далее):**
  * В сцене `0x03C` (Главная улица Лейквуда) 189 реплик жителей и спутников были пропущены в английском фанатском моде. Все 189 реплик переведены, пустые окна ликвидированы.
* **Секторный бюджет PROG.UNT и строгое ограничение сцены Сонии 0x03E:**
  * Донорский блок `0x011` содержит 164 свободных нулевых сектора (337 КБ).
  * Из него выделено 36 секторов для расширения диалоговых сцен (`0x03B` +4 сектора, `0x040` +5 секторов, `0x041` +4 сектора, `0x043` +3 сектора и др.).
  * **Сцена 0x03E (город Сония) оставлена строго 19 секторов (+0 секторов расширения, 38 912 байт)**. Любое расширение `0x03E` приводило к перезаписи таблицы указателей в RAM загружаемым следом контейнером `0x058` (адрес `0x8012A800`) и крашу при входе в Отель (подробный анализ в разделе 2.11). Русский текст `0x03E` оптимизирован до 37 848 байт (запас 1 064 Б, перекрытие строго 0 Б).
* **Шрифт диалогов:**
  * Установлен аутентичный 8-битный ретро-шрифт **Press Start 2P** (`fonts/PressStart2P.ttf`).
  * Кегль **11 pt**, базовая линия $y = 12$, горизонтальное центрирование. Строго $\le 15$ символов на строку, от 1 до 3 строк на страницу.
* **Выравнивание сюжетных диалогов с Ren'Py и ликвидация 216 урезанных строк (`tools/story_dialogue_overrides.py`):**
  * Скрипт `tools/align_renpy_to_po.py` выполнил сопоставление сюжета с фанатским переводом *Slayers Royal: Ren'Py Edition* (`renpy_extracted/game/script.rpy`) по структуре сцен и японским комментариям `#(JP) ...`.
  * В оригинальном движке PS1 для записей с опкодом `0x00FF` (запрет добавления страниц / одностраничный баббл) действует аппаратный лимит: максимум 3 строки по $\le 15$ символов ($\le 45$ символов суммарно). Длинные развёрнутые реплики из Ren'Py механически обрезались на границе третьей строки, оставляя повисшие союзы, точки и недосказанные фразы.
  * Создана база кураторских правок `tools/story_dialogue_overrides.py` (`POLISHED_OVERRIDES`), содержащая ровно **216 отшлифованных вручную реплик**. Каждая запись проверена на строгое соответствие правилам ($\le 15$ символов/строку, $\le 3$ строк), восстанавливает полный литературный смысл, юмор и естественную речь персонажей без обрезки и многоточий.
  * Все 216 записей сохранены на 100% даже при оптимизации бюджетов памяти сцены 0x03E.

---

### 2.3. Осмотр предметов в комнатах (`PROG.UNT 0x059..0x0F8`)
* **Мастер-каталог:** `translations/room_inspection_ru.json` — понятный человеку JSON-файл для удобного ручного контроля и редактирования.
* **Объем:** Все **149 интерактивных комнат** игры, **734 уникальные строки** (2 936 вхождений).
* **Восстановление 37 комнат из японского оригинала:**
  * Автор английского патча вырезал (заполнил нулями) описания в 37 комнатах игры, включая **MAIN ST (`0x059`)**.
  * Мы декодировали японский оригинал из `downloads/sr.bin` через `CHARMAP`, перевели все 43 строки улицы MAIN ST от лица Лины Инверс (*«Небо между деревьями»*, *«Колодец на площади»*, *«Обычный старик»*, *«Да земля же, говорю тебе!»*) и встроили в игру.
* **Устранение обрывов предложений («что...»):**
  * Все 143 фразы, которые ранее механически урезались на 15 символах, перефразированы в законченные, остроумные предложения (например: *«Вон то окно / открыто. Кинуть / туда камешек?»* вместо *«Окно открыто... камешек, что...»*).
* **Пакетный инжектор:** `tools/patch_inspection.py` с автоматическим пересчётом 32-битных указателей памяти (база `0x00200000`) и защитой от переполнения секторов.

---

### 2.4. Энциклопедические карточки персонажей (Lore Cards)
* **Мастер-каталог:** `translations/lore_cards_ru.json` (все 13 справочных карточек и 12 заголовков-баннеров; синхронизирован с `data/lore_cards_ru.json`).
* **Формат:** Полноэкранные текстуры оверлея формата **4bpp TIM** ($320 \times 224$ пикселя для 10 split-карточек) и **8bpp TIM** (для 3 комбинированных карточек 37, 38, 41), сжатые алгоритмом `unt_lz` mode 1 в `PROG.UNT` (записи 32, 34, 36, 37, 38, 40, 41, 43, 45, 47, 49, 51, 53) и баннеры в `OPT.UNT` (записи 182..193).
* **Шрифт карточек:** **Liberation Sans** (Bold для заголовков, Regular с тенью для описаний). Пропорциональный шрифт гарантирует отступ не менее 25-40 пикселей от правого края и исключает вылезание текста за границы экрана.
* **Инжектор:** `tools/patch_lore_cards.py`.

---

### 2.5. Видеоролики FMV (`MOVIE.STR`)
* **Расположение:** LBA 127..184416 (184 290 секторов, 433 МБ).
* **Количество:** 12 роликов (`s00`..`s11`).
* **Озвучка и субтитры:**
  * Видеоролики содержат японскую речь.
  * Все 11 сюжетных роликов (`s01`..`s11`) захардсаблены русскими субтитрами из `renpy_extracted/game/videos/s01_ru.srt`..`s11_ru.srt`.
* **Исправление маппинга потоков и вылета при выходе из Лейквуда:**
  * Порядок физических потоков в контейнере `MOVIE.STR` не является тривиальной последовательностью `0..11`: ролик `s01` (выход из Лейквуда) физически находится в 11-м потоке (сектор 153809), а потоки `s02`..`s11` идут со смещением (потоки 1..10).
  * Корректировка таблицы `MOVIE_MAP` в `tools/fmv_pipeline.py` устранила критический вылет/зависание движка при выходе из Лейквуда на карту мира и восстановила корректный запуск всех видеороликов в соответствии с вызовами игрового движка.
* **Устранение чёрного экрана (Seek Error fix):**
  * В `tools/bin/psxavenc` добавлен параметр `-L <starting_lba>`, благодаря чему каждый сектор ролика получает точный физический аппаратный заголовок **BCD MSF** (+150 pregap), совпадающий с положением дорожки (сектор 0 ролика 1 имеет строго `03:03:53`).
  * Флаг `-X` задаёт точное чередование секторов: **31 сектор видео $\to$ 1 сектор аудио (Interleave 1/32)**, что предотвращает опустошение буфера MDEC.
* **Инжектор:** `tools/fmv_pipeline.py`.

---

### 2.6. Боевой режим: перевод боевых диалогов (PROG.UNT 0x007, боевой шрифт 0x142)
* **Статус решения:** Разработана и внедрена выделенная, полностью изолированная система перевода боевых диалогов с поддержкой многостраничной пагинации:
  * **Отдельный файл перевода диалогов:** `translations/combat_dialogues_ru.json` — содержит **только** боевые диалоги (все 114 блоков диалогов, 264 реплики/баббла). Файл структурирован, понятен человеку и позволяет легко редактировать тексты, говорящих, реплики и страницы.
  * **Многостраничный вывод (Пагинация через опкод `0x00FD`):**
    * В боевом режиме диалоговое окно вмещает максимум 3 строки по $\le 21$ символа. Для длинных фраз реализована внутрибаббловая пагинация с помощью опкода **`0x00FD`** (`OPCODE_PAGE_BREAK` / `OPCODE_BUBBLE_ADVANCE`).
    * Опкод `0x00FD` выводит мигающую стрелочку-индикатор в правом нижнем углу баббла, ожидает нажатия кнопки подтверждения, очищает текстовую область и продолжает вывод следующей страницы без перезагрузки и мерцания портрета говорящего (повторный опкод портрета не передаётся).
    * В каталоге `translations/combat_dialogues_ru.json` пагинация задаётся либо явным массивом `"pages": ["стр. 1...", "стр. 2..."]`, либо символом смены страницы `\f` (`\x0c`) в поле `"text_ru"`.
    * Функция `validate_dialogue_formatting` строго проверяет каждую страницу реплики на лимиты ($\le 3$ строк, $\le 21$ символ на строку).
  * **Инжектор диалогов:** `tools/patch_combat_dialogues.py` — производит строгую in-place замену блоков в `0x05F810..0x06286A` с заполнением остатка блоков нулями (`0x0000`).
  * **Сохранение системных кнопок и меню (100% Zero UI Interference):** Кнопки управления в бою (`START`, `ATTACK`, `MOVE`, `PICK UNIT` и др., `0x05F278..0x05F470`), служебные таблицы Table 1, Table 2, Table 3 и машинный код MIPS (`0x02B78C` `jr $ra; nop`) оставлены **абсолютно нетронутыми**.
  * **Аппаратный 2BPP-формат шрифта 0x142:** Реверс-инжиниринг ассемблерного кода отрисовки `0x800989F8` в `PROG.UNT 0x007` показал, что движок боевого текста распаковывает глифы на лету из **2BPP** (2 бита на пиксель, 64 байта на тайл 16x16) контейнера с 4 банками по 256 тайлов:
    $$\text{offset} = 544 + \text{bank} \times 16384 + (\text{row} \times 16 + y) \times 64 + \text{col} \times 4$$
    Старый тулчейн ошибочно писал тайлы со страйдом 128 (как в 4bpp), из-за чего запись попадала мимо реальных тайлов, и движок считывал оригинальные японские кандзи (`0x0160` = `助`, `0x0180` = `流` и т.д.). Инъекция переписана на истинный 2BPP-формат движка.
  * **Шрифт 0x142 без конфликтов:** 66 русских букв (А-Я, а-я, Ё, ё) отрисованы в 2BPP шрифтом *Press Start 2P* в свободные ячейки `0x0150..0x0191`. Все латинские буквы, цифры и знаки препинания gourry-hacks сохранены неизменными. Сжатый размер шрифта — 40 986 байт (запас 6 118 байт до лимита 23 секторов / 47 104 байт).
  * **Проверка контрольных сумм:** Пересчитаны Mode 2 Form 1 EDC/ECC для 745 секторов Entry `0x007` и 23 секторов Entry `0x142`.
  * **Полное выравнивание боевых диалогов с Ren'Py и восстановление голосов персонажей:**
    * Все **114 блоков боевых диалогов** (264 реплики) сопоставлены с оригинальными японскими комментариями `#(JP) ...` и репликами из скрипта Ren'Py (`renpy_extracted/game/script.rpy`, от `call start_battle_eff` до `call battle_victory_sequence`).
    * **Восстановление аутентичных голосов персонажей:** Устранён сухо-технический перевод; полностью восстановлены характерный комедийный тон, интонации и лексика героев новеллы (Лина, Нага, Гаури, Зелгадис, Амелия, Сильфиль, Ларк, Галеф).
    * **Строгий контроль бюджетов и Zero Overflow (0 переполнений):** Все 114 блоков строго укладываются в оригинальные in-place границы секторов `0x05F810..0x06286A` в `PROG.UNT 0x007` (выделено 12 378 байт, использовано 11 840 байт, суммарный запас 538 байт, переполнений — 0).

---

### 2.7. Названия локаций и карта мира (PROG.UNT 0x001 и BASYOG.UNT 466)
* **Статус решения:** Полностью переведены на русский язык обе подсистемы навигации и отображения локаций:
  1. **Названия локаций на глобальной карте (World Map Navigation Text):**
     * **Каталог:** `translations/world_map_ru.json` (все 20 пунктов назначения и направлений).
     * **Размещение:** `PROG.UNT` Entry 1 (`0x001`), сектор 89 (**LBA 229109**), смещение `0x05FA` (дисковое смещение `0x205FDF82`).
     * **Таблица указателей:** Сектор 93 (**LBA 229113**), смещение `0x0120`..`0x016C` (20 32-битных указателей памяти RAM `0x8005C...`).
     * **Переведённые строки:** `LAKEWOOD` $\to$ **«ЛЕЙКВУД»**, `BARK LAND AREA` $\to$ **«ОБЛ. БАРКЛЕНД»**, `SAILLUNE` $\to$ **«СЕЙРУН»**, `FREEGROUND` $\to$ **«ФРИГРАНТ»**, `TRUE CITY AREA` $\to$ **«ОБЛ. ТУР-СИТИ»**, `LAKEWOOD S. FOREST` $\to$ **«Ю. ЛЕС ЛЕЙКВУДА»**, `BARKLAND PLAZA` $\to$ **«ПЛОЩАДЬ БАРКЛЕНДА»**, `BARKLAND MAIN ST` $\to$ **«ГЛ. УЛ. БАРКЛЕНДА»** и др.
     * **Инжектор:** `tools/patch_world_map.py` с автоматическим пересчётом указателей RAM и EDC/ECC контрольных сумм.
     * **Бюджет:** Таблица занимает 446 байт из доступных 458 байт сектора 89 (запас 12 байт; критическая таблица перехода BIOS/SPU в `0x07C6..0x0800` защищена и восстанавливается автоматически).
  2. **Графические плашки и вывески локаций (Location Banners):**
     * **Размещение:** `BASYOG.UNT` Entry 466 (**LBA 240133..240140**, 8 секторов = 16 384 байта, дисковое смещение `0x21AFD230`).
     * **Формат:** 4bpp PS1 TIM ($256 \times 224$ пикселя, 16 цветов CLUT), сжатый алгоритмом LZSS (`unt_lz` mode 1).
     * **Каталог перевода:** `translations/location_banners_ru.json` — отдельный настраиваемый пользователем JSON-каталог всех 23 плашек с гибким управлением типографикой (`font_size`, `align`, `offset_x`, `offset_y`, `max_hardware_width_px`).
     * **Отрисовка:** Пиксельные надписи с тёмной обводкой: `ГЛАВНАЯ`, `ПЛОЩАДЬ`, `ЗАКОУЛКИ`, `ТАВЕРНА`, `ОТЕЛЬ`, `БАР`, `ОРУЖЕЙНАЯ`, `ЦЕРКОВЬ`, `ОКРАИНА`, `ПОКИНУТЬ ГОРОД`, `ДВОРЕЦ`, `ДОМ МЭРА`, `ДОМ ЛАРКА`, `ГИЛЬДИЯ`.
     * **Инжектор:** `tools/patch_location_banners.py` с сохранением превью в `data/preview_basyog_466_ru.png`.

---

### 2.8. Городские службы: таверны, отели, магазины и валюта (PROG.UNT 0x003, 0x03A)
* **Статус решения:** Полностью переведены на русский язык меню городских служб и подсистема цен:
  * **Размещение:** `PROG.UNT` Entry 3 (`0x003`), LBA 229219..229514 (296 секторов = 606 208 байт, RAM base `0x8004E5B0`).
  * **Мастер-каталог:** `translations/town_services_ru.json` — структурированный JSON для управления всеми городскими меню:
    1. **Еда в таверне (Tavern Dining, 0x2D414..0x2D4E0):**
       * `FULL MEAL` $\to$ **«СЫТНЫЙ ОБЕД»** (указатель `0x02D524` $\to$ `0x8007B9C4`).
       * `SNACK` $\to$ **«ПЕРЕКУС»** (безопасно перемещён в `0x02D510`, указатель `0x02D528` $\to$ `0x8007BAC0`).
       * Статусы: `No money...` $\to$ **«Нет денег...»**, `You just ate.` $\to$ **«Мы только поели»**.
       * **Защита таблицы кью таверны (Tavern City Cue Table 0x02D4CC):** Строка `status_just_ate` строго ограничена бюджетом $\le 32$ байт, что исключило наложение на служебную таблицу городов по смещению `0x02D4CC`.
    2. **Ночлег в отеле (Inn Lodging, 0x2DF8C..0x2E020):**
       * `REST NIGHT` $\to$ **«ОТДЫХ ДО НОЧИ»** (указатель `0x02DFD4` $\to$ `0x8007C53C`).
       * `SLEEP MORN` $\to$ **«СОН ДО УТРА»** (указатель `0x02DFD8` $\to$ `0x8007C558`).
       * Сохранена критическая таблица перехода отелей `Hotel City Cue Table` (`0x02DF70..0x02DF88`), включая вызов отеля Сонии `0x04A6` (`0x02DF78`).
    3. **Валюта и ценники (Currency Headers & Suffix):**
       * Заголовок серебра (`0x00BD0`): композитные 4bpp-тайлы `0x0242` + `0x0244` в `PROG.UNT 0x03A` отрисованы как **«СЕРЕБРО»** (`[СЕР]` + `[ЕБРО]`).
       * Заголовок золота (`0x00BD8`): композитные тайлы `0x024A` + `0x024B` отрисованы как **«ЗОЛОТО»** (`[ЗОЛ]` + `[ОТО]`).
       * Заголовок меди (`0x00BC8`): композитные тайлы `0x0254` + `0x0259` отрисованы как **«МЕДЬ»** (`[МЕД]` + `[Ь]`).
       * Суффикс ценника в машинном коде MIPS (`0x0221F8`): инструкция `li $v0, 0x01FD` (ASCII 'X') заменена на `li $v0, 0x007D` (пробел), ликвидировав артефакт `'X'` во всех ценниках (`СЕРЕБРО 5` вместо `SILVER 5X`).
    4. **Меню магазинов и торговцев (0x30BD0..0x322AA):**
       * Системные иконки: `ВЫХОД`, `КУПИТЬ`, `ПРОДАТЬ`, `КАРТА`, `ОТЕЛЬ`, `ЗАКАЗ`, `СОХРАНЕНИЕ`, `ЗАГРУЗКА`, `ОПЦИИ`, `ВОЙТИ`.
       * Торговые диалоги: `КОМУ?`, `ЧТО?`, меню `ЦЕНА / ОТМЕНА`, `КУПИТЬ / ТОРГ / ОТМЕНА`, реплики торга Лины, Гаури, Зелгадиса, Амелии и торговцев.
  * **Инжектор:** `tools/patch_town_services.py` с автоматическим обновлением указателей RAM и пересчётом Mode 2 Form 1 EDC/ECC.

---

### 2.9. Мини-игры и викторина: полная синхронизация с Ren'Py и ликвидация моджибейка
* **Статус решения:** Полностью переведены на русский язык правила, заголовки, интерфейс, внутриигровые диалоги и статусные сообщения всех 5 мини-игр, а также все 100 вопросов викторины. Устранены все проявления моджибейка (каракулей) и аппаратных крашей:
  * **Мастер-каталог перевода:** `translations/minigames_ru.json` — единый JSON-каталог, содержащий тексты правил, заголовков, диалогов, статусных реплик, комбинаций геймпада и 100 вопросов викторины.
  * **Выравнивание викторины с Ren'Py (`renpy_extracted/game/quiz.rpy`):**
    * Все **100 вопросов викторины** сопоставлены с фанатским переводом *Slayers Royal: Ren'Py Edition* (`ps1_index: 0..99` $\leftrightarrow$ `renpy_id: 1..100`).
    * **18 вариантов ответов и 5 вопросов выровнены с Ren'Py** для полного соответствия канону новеллы и исключения смысловых расхождений.
    * Индексы правильных ответов (`correct: 0, 1 или 2`) строго выверены по исходнику Ren'Py (`correct_ru`).
    * Описание правил викторины перенесено из оригинального фанатского перевода: *«Ответьте правильно на все 100 вопросов по вселенной «Рубак». Вопросы будут идти в случайном порядке. Допускается не больше двух ошибок. Если не уложитесь по времени, это тоже считается ошибкой.»*
    * Вопросы (`q_line1`, `q_line2`) и варианты ответов (`opt1`, `opt2`, `opt3`) строго ограничены $\le 15$ символами на слот ($15 \times 16 = 240$ px $\le 320$ px экрана PS1), исключая обрезку текста.
  * **Архитектура непрерывного потока правил (Unified Continuous Stream):**
    * Заголовок и правила объединены в единый непрерывный 16-битный LE поток:
      $$\text{[0x0000]} + \text{encode(title)} + \text{[0x00FD, 0x00FD]} + \text{encode(rules)} + \text{[0x00FF]} + \text{[0x00 padding]}$$
    * Устранён преждевременный терминатор `0x00FF` после заголовка (zero premature `0x00FF`), благодаря чему плашка и правила под ней гарантированно отображаются на экране одновременно.
    * Бюджеты потоков правил:
      * **Викторина (Entry 16 @ 0x08184):** 252 байта (занято 224 Б, запас 28 Б). «Викторина:» + 7 строк правил.
      * **Обед (Entry 13 @ 0x07320):** 328 байт (занято 260 Б, запас 68 Б). «Обед:» + 7 строк правил.
      * **Прыжок Амелии (Entry 14 @ 0x11318):** 354 байта (занято 302 Б, запас 52 Б). «Прыжок:» + 8 строк правил.
      * **Смех Наги (Entry 15 @ 0x07EB4):** 274 байта (занято 250 Б, запас 24 Б). «Смех Наги:» + 7 строк правил.
      * **Отстрел бандитов (Entry 17 @ 0x082B8):** 346 байт (занято 306 Б, запас 40 Б). «Отстрел:» + 9 строк правил.
  * **Смех Наги (PROG.UNT Entry 15): перевод всех 6 диалогов и ликвидация английских байтов:**
    * В английском фанатском моде диалоги Наги были закодированы 1-based ASCII-смещениями (`A=1..Z=26`), что при инъекции русского шрифта приводило к абракадабре `У О Ь И П...`.
    * Переведены все 6 диалоговых блоков (`0x07FC8..0x0833A`) с поддержкой смены страниц через опкод `0x00FE` (`\f`):
      * `dialogue_1` (0x07FC8, бюджет 162 Б): *«Ну ладно, / я помогу!\fС великой силой / Наги враг нам / нипочём!»* (Перед мини-игрой).
      * `dialogue_2` (0x0806A, бюджет 170 Б): *«Жалкое зрелище! / Неужто думали / одолеть меня?\fОтдайте награду / или молите / о пощаде!»* (Победа / Рекорд).
      * `dialogue_3` (0x08114, бюджет 168 Б): *«Насмехаетесь над / моим нарядом? / Какая дерзость!\fКрасавица верна / своему стилю!»* (Реакция на наряд).
      * `dialogue_4` (0x081BC, бюджет 164 Б): *«Тогда у меня / не было денег! / Каждый ошибается!\fНе цепляйся, / а не то пожалеешь!»* (Реакция на долг).
      * `dialogue_5` (0x08260, бюджет 166 Б): *«Подумаешь, толчок! / Что за шум? / Какое ребячество!\fВсякое бывает / по воле судьбы!»* (Реакция на толчок).
      * `dialogue_6` (0x08306, бюджет 52 Б, текст 0x0831C): *«Нага довольна!»* (Статусная строка Наги).
    * Сохранена служебная таблица 5 указателей RAM `NAGA_DIALOGUE_POINTERS` (`0x80056578`, `0x8005661C`, `0x800566C4`, `0x8005676C`, `0x80056810`) по смещению `0x08308..0x0831C`.
  * **Прыжок Амелии (PROG.UNT Entry 14): перевод пафосных речей Справедливости:**
    * Переведены все 5 героических речей Амелии (`0x1147C..0x11850`) и финальная строка:
      * `dialogue_1` (0x1147C, 200 Б, RAM `0x8005FA2C`): *«Где есть жизнь — / свет и тьма / неразлучны!\fГде есть добро, / будет и зло!\fУзрите же кару / Амелии!»*
      * `dialogue_2` (0x11544, 168 Б, RAM `0x8005FAF4`): *«Довольно зла! / Один лишь луч / чистого света\fв каждом сердце / рассеет тьму!»*
      * `dialogue_3` (0x115EC, 226 Б, RAM `0x8005FB9C`): *«Лжецы! / Ваш обман никогда / не сломит мою / веру в закон!\fЗло сгинет, / и мы победим!»*
      * `dialogue_4` (0x116D0, 226 Б, RAM `0x8005FC80`): *«Разбойники! / Жадность затмила / ваш разум!\fПощады не ждите! / Узрите пламя / правосудия!»*
      * `dialogue_5` (0x117B4, 160 Б, RAM `0x8005FD64`): *«Трепещи, зло! / Меня не провести!\fЗакон может / ошибиться, / но я — никогда!»*
      * `justice_up` (0x12318, 32 Б, RAM `0x800608DE`): **«Справедливость!»**.
    * Полностью устранён артефакт японского кандзи `生` (`0x0163`), возникавший в начале первой речи Амелии.
    * Сохранена таблица из 5 RAM-указателей `AMELIA_DIALOGUE_POINTERS` по смещению `0x11854..0x11868`.
  * **Вертикальное окно комбинаций поз Амелии: восстановление иконок кнопок и устранение кракозябр:**
    * **Причина бага:** В окне поз Амелии выводились кракозябры `э г ь / Д О ( ь / У г...`. Это происходило из-за того, что оригинальный шрифт мини-игры в `SMINI.UNT` содержал иконки кнопок геймпада на позициях `0x006B` (кнопка `○`), `0x0074` (`↑`), `0x0075` (`↓`), `0x0076` (`→`), `0x0077` (`←`). При инъекции кириллицы эти ячейки затирались глифами букв.
    * **Защита контроллерных глифов (`SMINI_PROTECTED_GLYPHS`):** Диапазон `0x0057..0x0076` (латиница k..z и катакана) исключён из перезаписи кириллицей. Оригинальные тайлы кнопок `○`, `↑`, `↓`, `→`, `←` в TIM-шрифте Entry 1 полностью восстановлены и защищены.
    * **Перевод потоков комбинаций:** Введены русские метки уровней поз: `ОК:` (Header), `УРА:` (Level A), `УРБ:` (Level B), `УРВ:` (Level C), `УРС:` (Level S):
      * `difficulty_1` (Легко, 0x11868, бюджет 92 Б, RAM `0x8005FE18`): `ОК:` + `○`, `УРА:` + `↓ ○`, `УРБ:` + `↓ → ○`, `УРВ:` + `↓ → ↓ ○`, `УРС:` + `?`.
      * `difficulty_2` (Нормально, 0x118C4, бюджет 100 Б, RAM `0x8005FE74`): `ОК:` + `○`, `УРА:` + `↓ ↓ ○`, `УРБ:` + `← ↓ → ○`, `УРВ:` + `← → ↓ ← ○`, `УРС:` + `?`.
      * `difficulty_3` (Сложно, 0x11928, бюджет 100 Б, RAM `0x8005FED8`): `ОК:` + `○`, `УРА:` + `↑ ↓ ○`, `УРБ:` + `← ↑ → ↓ ○`, `УРВ:` + `← → ↓ → ○`, `УРС:` + `?`.
    * Сохранена 3-указательная таблица `AMELIA_COMBO_POINTERS` (`0x8005FE18`, `0x8005FE74`, `0x8005FED8`) по смещению `0x1198C..0x11998`.
  * **Обед (PROG.UNT Entry 13): статусные реплики Лины и Гаури:**
    * Переведены 3 внутриигровые статусные реплики (`0x0746C..0x07532`):
      * `lina_morale` (0x0746C, 72 Б, RAM `0x80055A1C`): *«Лина в ударе! / Дух крепчает! / Рвение!»*
      * `gourry_satiated` (0x074B4, 28 Б, RAM `0x80055A64`): *«Гаури сыт!»*
      * `combined_status` (0x074D0, 98 Б, RAM `0x80055A80`): *«Лина в ударе! / Дух и рвение взмыли! / Гаури сыт!»*
  * **Отстрел бандитов (PROG.UNT Entry 17): бонус времени:**
    * Переведена строка бонуса времени (`0x08414..0x08428`):
      * `time_bonus` (0x08414, 20 Б, RAM `0x800569C4`): **«Время!»**.
  * **Шрифтовая подсистема SMINI.UNT и пересчёт контрольных сумм:**
    * Инъекция 139 кириллических тайлов Press Start 2P во все 5 приватных TIM-шрифтов `SMINI.UNT` (Entries 0..4, VRAM адреса `(960,0)`, `(896,256)`, `(320,256)`, `(704,256)`).
    * Строгий in-place пересчёт EDC/ECC CD-ROM XA Mode 2 Form 1 для всех **1 887 секторов** диска (379 секторов `PROG.UNT` и 1 508 секторов `SMINI.UNT`).

---

### 2.10. Мастер-каталог сюжетных диалогов и конвейер сборки
* **Статус решения:** Разработана современная, отказоустойчивая подсистема ведения и сборки сюжетных диалогов:
  * **Мастер-каталог `translations/story_dialogues_ru.json`:**
    * Содержит все **4 514 сюжетных реплик** во всех **30 сценах** игры (`0x03B..0x057`, `0x139`).
    * Структурирован в иерархию сцен с полными метаданными:
      * `scene_id`: шестнадцатеричный идентификатор сцены (`03B`, `03C`, ...).
      * `family`: тип разметки (`tagged`, `special`).
      * `title`: понятное человеку название локации и сюжетного момента.
      * `dialogues`: список реплик с полями `context`, `speaker`, `speaker_ru` (по словарю `SPEAKERS_GLOSSARY`: Лина, Гаури, Нага, Зелгадис, Амелия, Сильфиль, Ларк, Эмилия), `jp_source`, `text_ru`, `allow_page_break`, `controls`.
  * **Утилита двусторонней синхронизации `tools/sync_story_dialogues.py`:**
    * Осуществляет бесшовную синхронизацию между `translations/story_dialogues_ru.json` и компиляторным каталогом `patch_repo/localization-work/ru/dialogue.po`.
    * **Аппаратная валидация PS1 (`--validate`):**
      * Нормализация Unicode в форму NFC.
      * Запрет символов возврата каретки `\r`.
      * Длина каждой строки: строго $\le 15$ символов.
      * Количество строк на страницу: строго от 1 до 3 строк (не допускаются пустые строки).
      * Правила символа смены страницы `\f`: строго запрещён для сегментов с терминатором `0x00FF` (одностраничные бабблы).
    * **Режимы работы:**
      * `--export`: первичный экспорт `dialogue.po` $\to$ `translations/story_dialogues_ru.json`.
      * `--apply`: валидация и накат изменений из JSON в `dialogue.po`.
      * `--sync-if-newer`: сравнение mtime файлов; если JSON новее PO, автоматически накатывает изменения перед сборкой.
      * `--validate`: комплексная проверка всех 4 514 реплик каталога.
      * `--status`: сводная статистика по персонажам, сценам и датам модификации.
  * **Интеграция в сборочный скрипт `build.sh`:**
    * На этапе `[1/9]` автоматически вызывается `tools/sync_story_dialogues.py --sync-if-newer`.
    * Добавлен флаг **`./build.sh --story`** (синоним `--dialogues`) для быстрой изолированной пересборки сюжетных диалогов и базового образа (~12 секунд вместо 38 секунд полной сборки).
    * Валидация диалогов интегрирована в общую команду проверки проекта `./build.sh --validate`.
  * **Единый реестр каталогов `translations/translations_index.json`:**
    * Содержит спецификации всех 10 каталогов локализации в `translations/`:
      1. `story_dialogues` (`translations/story_dialogues_ru.json`) — 4 514 сюжетных реплик.
      2. `combat_dialogues` (`translations/combat_dialogues_ru.json`) — 114 блоков боевых диалогов.
      3. `room_inspection` (`translations/room_inspection_ru.json`) — 149 интерактивных комнат, 734 строки.
      4. `minigames` (`translations/minigames_ru.json`) — 5 мини-игр и 100 вопросов викторины.
      5. `town_services` (`translations/town_services_ru.json`) — 417 строк городских служб, отелей, меню.
      6. `shop_dialogues` (`translations/shop_dialogues_ru.json`) — 98 диалогов и меню лавок (100 указателей).
      7. `lore_cards` (`translations/lore_cards_ru.json`) — 13 карточек персонажей и 12 баннеров.
      8. `world_map` (`translations/world_map_ru.json`) — 20 названий локаций глобальной карты.
      9. `location_banners` (`translations/location_banners_ru.json`) — 24 графические плашки локаций.
      10. `room_names` (`translations/room_names_ru.json`) — 160 названий локаций и комнат игры (PROG.UNT 0x059..0x0F8).

---

### 2.11. Архитектура памяти сцен и ликвидация коллизии в Сонии (0x03E vs 0x058 at 0x8012A800)
* **Статус решения:** Устранён критический баг игрового движка PS1, приводивший к чёрному экрану при загрузке города Сония и вылету эмулятора при попытке ночлега в Отеле Сонии:
  * **Архитектура буферов сцены в RAM:**
    * В игровом движке *Slayers Royal* текущая сюжетная сцена распаковывается и загружается в память RAM начиная с базового адреса **`0x80121000`**.
    * Аппаратный потолок этого буфера ограничен 44 384 байтами (до `0x8012BD80`).
    * Однако следующий программный модуль игры — системный контейнер скриптов комнат и событий **`PROG.UNT 0x058`** — загружается в память по строго захардкоженному аппаратному адресу:
      $$\text{RAM Address of 0x058} = \mathbf{0x8012A800}$$
  * **Математический расчёт критического лимита:**
    $$\Delta = 0x8012A800 - 0x80121000 = 0x9800 = 38\,912 \text{ байт}$$
    $$\text{Секторов CD-ROM} = \frac{38\,912 \text{ байт}}{2\,048 \text{ байт/сектор}} = \mathbf{19 \text{ секторов}}$$
  * **Причина краша (Root Cause):**
    * В оригинальной японской версии сцена Сонии `0x03E` занимала ровно 19 секторов (38 912 байт) и идеально стыковалась с блоком `0x058`.
    * При переводе на русский язык в ранних версиях патчера сцене `0x03E` было выделено +13 секторов расширения (всего 32 сектора = 65 536 байт) из донорского блока `0x011`.
    * При входе в город Сония сцена `0x03E` загружалась по адресам `0x80121000..0x80131000`.
    * Вслед за ней движок загружал контейнер `0x058` по адресу `0x8012A800`.
    * В результате блок `0x058` **намертво перезаписывал хвост сцены 0x03E** (`0x8012A800..0x80131000`), где располагалась таблица 32-битных указателей реплик сцены (`pointers`).
    * При входе в отель Сонии городская служба вызывала отельный кью `0x04A6`, ссылавшийся на запись `0x00C5` в таблице указателей `0x03E`.
    * Вместо валидного 32-битного выровненного указателя памяти `0x0023XXXX` игра считывала мусорные байты `0x7FF04F6D` / `0x7FEEC283`.
    * Процессор MIPS R3000A выбрасывал `Address Error Exception (Load/Store)` при попытке чтения инструкции по невыровненному адресу за пределами RAM, что приводило к моментальному зависанию и чёрному экрану.
  * **Инженерное решение:**
    1. **Строгое закрепление 19 секторов для 0x03E:** В `patch_repo/localization/script.py` расширение для `0x03E` убрано из `EXTRA_EXPANSION_SECTORS` (`0x03E: 0 секторов`). Размер сцены `0x03E` на диске и в RAM составляет строго **19 секторов (38 912 байт, 0x80121000..0x8012A800)**. Перекрытие с блоком `0x058` равно **строго 0 байт**.
    2. **Оптимизация текста без потери качества:** Утилитой `tools/fit_dialogue_budget.py` русский текст сцены `0x03E` оптимизирован до **37 848 байт**. Свободный запас внутри 19 секторов составляет **1 064 байта**.
    3. **100% сохранение базы overrides:** Все **216 отшлифованных вручную реплик `POLISHED_OVERRIDES`** в сцене Сонии сохранены полностью и без сокращений.
    4. **Защита таблицы перехода таверны (0x02D4CC):** В `tools/patch_town_services.py` исправлено наложение буфера строки `status_just_ate` («Мы только поели»), который ранее залезал на 32 байта в таблицу городов таверны `0x02D4CC`. Буфер строго ограничен $\le 32$ байт, целостность таблицы переходов восстановлена.
    5. **Синхронизация сейвстейта эмулятора:** В файле сохранения `SLPS-01363_6.sav` дамп RAM синхронизирован с новыми адресами и валидными указателями, исключив использование устаревшего испорченного состояния памяти.


---

### 2.12. Диалоги магазинов и лавок: ликвидация моджибейка («ЩФР ПХИПР?») и синхронизация шрифта (PROG.UNT 0x003, 0x03A)
* **Статус решения:** Полностью переведена и интегрирована подсистема магазинов и лавок, устранён моджибейк реплик торговцев и торга персонажей:
  * **Размещение и указатели:**
    * `PROG.UNT` Entry 3 (`0x003`), LBA 229219..229514 (296 секторов, RAM base `0x8004E5B0`).
    * **Table 1 (0x031F14..0x032060, 84 указателя):** Диалоги покупки, приветствия торговцев, реакции на торг Лины (`<9101>`), Гаури (`<9141>`), Зелгадиса (`<9161>`), Амелии (`<9181>`), Сильфиль (`<91A1>`), Ларка (`<91C1>`), реплики 3 типов торговцев (Оружейник `<D9A1>`, Торговка предметами `<D9C1>`, Торговец магией `<D9E1>`), контрпредложения и меню скидок.
    * **Table 2 (0x0322B0..0x0322EC, 16 указателей):** Диалоги продажи, оценка предметов, отказы, завершение сделок и благодарности.
    * **Итого:** 98 уникальных текстовых слотов, охватывающих все **100 указателей**.
  * **Причина бага моджибейка («ЩФР ПХИПР?», Root Cause):**
    * В `tools/patch_town_services.py` импортировался устаревший `DEFAULT_CHARMAP` из `tools/patch_inspection.py`, где кириллические символы имели ошибочные сдвинутые коды (`Ч` = 0x0021 вместо 0x0020, `т` = 0x003E вместо 0x003D, `о` = 0x0039 вместо 0x0038, `н` = 0x0038 вместо 0x0037, `у` = 0x003F вместо 0x003E, `ж` = 0x0030 вместо 0x002F и т.д.).
    * В то же время растровый шрифт VRAM, загружаемый из `PROG.UNT 0x03A`, использует эталонное распределение `glyph_map.json` / `AUTHORITATIVE_CYRILLIC_CHARMAP` (где 0x0021 = `Щ`, 0x001C = `Ф`, 0x0018 = `Р`).
    * В результате строка приветствия оружейника `<D9A1>Что нужно?` при кодировании превращалась в `0x0021, 0x003E, 0x0039...` и рендерилась на экране как **«ЩФР ПХИПР?»**.
  * **Выделенный каталог перевода (`translations/shop_dialogues_ru.json`):**
    * Создан человекочитаемый JSON-каталог, содержащий:
      * `dialogues`: 98 диалоговых записей с полями `id`, `offset_hex`, `pointer_offsets_hex`, `speaker_code_hex`, `speaker`, `category`, `text_jp`, `text_ru`, `max_bytes`.
      * `shop_items`: 91 наименование товаров, персонажей и параметров из таблицы `0x030A20` (указатели `0x030A20..0x030B88`, строки `0x03054C..0x030A10`).
    * Поддержана динамическая подстановка кодов спикеров: при наличии `speaker_code_hex` код автоматически предваряет реплику, позволяя хранить в `text_ru` чистый текст.
    * Сохранены все системные переменные движка: `<00BF>` (название предмета, параметр D) и `<00BE>` (число золотых монет, параметр A).
    * Соблюдены аппаратные лимиты PS1 для окон диалогов: строго $\le 15$ символов в строке ($15 \times 16 = 240$px ширина окна), 1..3 строки на страницу, перенос строк `\n`, смена страниц `\f`. При превышении 15 символов происходит переполнение GPU-пакета координат в RAM с отрисовкой мусорных опкодов поверх текста.
  * **Устранение бага наложения оверлея оружия (Hardware Overlay Token `<00BF>`):**
    * **Причина бага наложения:** Движок PS1 при выводе токена `<00BF>` рисует название рекомендованного оружия/предмета аппаратным оверлеем по фиксированным координатам экрана:
      * Для одиночного токена в `buy_suggest_weapon` координаты захардкожены на $X = -128, Y = 56$ (начало 2-й строки окна). Если во 2-й строке перед `<00BF>` стоял русский текст (`то есть <00BF>`), оверлей рендерился прямо поверх этих букв. Токен `<00BF>` вынесен строго в начало 2-й строки: `Есть получше:\n<00BF>.\nКак тебе?`.
      * Для двойного токена в `buy_material_weapon` («оружие из материала»): Токен 1 рисуется в начале 1-й строки, Токен 2 — аппаратным оверлеем в начале 2-й строки ($Y = 56$). Ранее Токен 2 стоял в конце 3-й строки, из-за чего он отрисовывался поверх русской 2-й строки (`Есть отличная вещь`). Строка переформатирована так, что Токен 1 стоит в начале 1-й строки, а Токен 2 — строго в начале 2-й: `<00BF>, говоришь?\n<00BF> как раз есть.\nБерёшь?`. Во всех остальных диалогах с `<00BF>` токен также выровнен по началу строк.
    * **Компактные сокращения характеристик (Таблица 0x030A20, слоты 07..12):** В красном окне сравнения характеристик ширина колонок ограничена 6–7 символами. Длинные строки (10–11 символов) слипались с числовыми значениями статов (`Физ. атак0`), а строка `Уклонение` вылезала за правую рамку окна и залипала на экране (`ть Уклонени`). Метки характеристик сокращены до компактных 5-символьных обозначений: `[07]` `Ф.Атк`, `[08]` `Ф.Защ`, `[09]` `М.Атк`, `[10]` `М.Защ`, `[11]` `Точн.`, `[12]` `Уклон`.
    * **Перевод названий товаров (Таблица 0x030A20):** Токен `<00BF>` загружает строковое имя из таблицы 91 указателя `0x030A20`. Все 91 наименование переведены на русский язык и пропатчены в образ диска и сейвстейты.
    * Реплики, превышающие размер исходных слотов, автоматически размещаются в свободную память Entry 3 с пересчётом всех связанных 32-битных указателей в RAM.
---

## 3. Файловая структура проекта

```text
dddd/
├── translations/
│   ├── translations_index.json   # ЕДИНЫЙ РЕЕСТР: спецификации всех 9 каталогов локализации
│   ├── story_dialogues_ru.json   # РУЧНОЙ КОНТРОЛЬ: 4 514 сюжетных диалогов (30 сцен, спикеры, JP-оригинал)
│   ├── combat_dialogues_ru.json  # РУЧНОЙ КОНТРОЛЬ: 114 блоков боевых диалогов (264 реплики)
│   ├── room_inspection_ru.json   # РУЧНОЙ КОНТРОЛЬ: все 734 строки описания предметов в 149 комнатах
│   ├── minigames_ru.json         # РУЧНОЙ КОНТРОЛЬ: правила 5 мини-игр, 100 вопросов викторины, диалоги Наги и Амелии
│   ├── town_services_ru.json     # РУЧНОЙ КОНТРОЛЬ: таверны (FULL MEAL/SNACK), отели, магазины, валюта
│   ├── shop_dialogues_ru.json    # РУЧНОЙ КОНТРОЛЬ: 98 диалогов и меню лавок, торг персонажей (100 указателей)
│   ├── lore_cards_ru.json        # РУЧНОЙ КОНТРОЛЬ: тексты 13 справочных карточек персонажей
│   ├── world_map_ru.json         # РУЧНОЙ КОНТРОЛЬ: 20 названий локаций на карте мира
│   ├── location_banners_ru.json  # РУЧНОЙ КОНТРОЛЬ: 24 плашки локаций (текст, кегль, выравнивание, смещения)
│   └── combat_ru.json            # Экспериментальный общий файл системных строк и диалогов
├── data/
│   ├── gourry_latin_tiles.json   # Эталонные латинские тайлы боевого шрифта gourry-hacks
│   ├── missing_rooms_jp_ru.json  # Перевод 37 пропущенных комнат (включая MAIN ST)
│   └── preview_*.png             # Визуальные дампы проверки графики, плашек и видео
├── downloads/
│   ├── sr.bin                    # Чистый японский Redump-образ (712 300 848 B)
│   └── sr.cue                    # Точный CUE-файл (68 B)
├── fonts/
│   └── PressStart2P.ttf          # Пиксельный ретро-шрифт для диалогов
├── localization-output/ru/
│   ├── slayers_royal_ru.bin      # ФИНАЛЬНЫЙ РУССКИЙ ОБРАЗ ДИСКА (712 300 848 B)
│   └── slayers_royal_ru.cue      # CUE для запуска в эмуляторе
├── patch_repo/                   # Инструментарий патчера gourry-hacks
│   ├── localization-work/ru/
│   │   └── dialogue.po           # 4 514 сюжетных реплик и меню (синхронизируется с JSON)
│   ├── localization/             # Модули сборки диска, шрифтов и скриптов
│   └── localize.py               # CLI сборщика диалогов
├── tools/
│   ├── bin/
│   │   ├── psxavenc              # Модифицированный MDEC/CD-XA кодировщик с -L и -X
│   │   ├── DuckStation.AppImage  # Эмулятор PS1
│   │   └── bios/                 # Биосы scph5500.bin и scph5501.bin
│   ├── sync_story_dialogues.py   # Двусторонняя синхронизация PO <-> JSON и аппаратная валидация PS1
│   ├── story_dialogue_overrides.py # База 216 кураторских реплик для записей 0x00FF (строго <=15 симв/стр, <=3 строк)
│   ├── align_renpy_to_po.py      # Инструмент нормализации и выравнивания Ren'Py v0.9.9 с dialogue.po
│   ├── fit_dialogue_budget.py    # Оптимизатор бюджетов диалоговых сцен (сжатие 0x03E до 37 848 B)
│   ├── patch_minigames.py        # Инжектор правил, 100 вопросов, диалогов Наги/Амелии и шрифтов SMINI.UNT (1 887 секторов)
│   ├── patch_combat_dialogues.py # Выделенный инжектор боевых диалогов (114 блоков, zero-interference, пагинация 0x00FD)
│   ├── combat_dialogue_charmap.py# Бесконфликтная кодовая страница, тайлы 0x142 и опкод пагинации 0x00FD
│   ├── analyze_combat_budgets.py # Анализ бюджетов (114 блоков), триггеров и симулятор репакинга диалогов 0x007
│   ├── patch_combat_font.py      # Генератор боевого шрифта 0x142 (палитра 3/1/0, Press Start 2P)
│   ├── combat_text.py            # Парсер и кодировщик боевых строк (синхронизирован с 0x03A glyph_map.json)
│   ├── patch_town_services.py    # Инжектор городских служб (таверны, отели, магазины, валюта 0x003, 0x03A)
│   ├── build_shop_catalog.py     # Генератор и валидатор каталога диалогов магазинов (98 записей)
│   ├── test_shop_catalog.py      # Тесты каталога диалогов лавок (покрытие 100 указателей, лимиты, чармап)
│   ├── patch_inspection.py       # Пакетный инжектор 149 комнат (PROG.UNT 0x058..0x138)
│   ├── patch_lore_cards.py       # Инжектор карточек персонажей (Liberation Sans, 4bpp/8bpp TIM)
│   ├── patch_world_map.py        # Инжектор карты мира (PROG.UNT 0x001 LBA 229109/229113)
│   ├── patch_location_banners.py # Инжектор графических плашек (BASYOG.UNT 466 LBA 240133)
│   ├── fmv_pipeline.py           # Пайплайн кодирования и инъекции 12 FMV-видео в MOVIE.STR
│   ├── test_sync_story_dialogues.py # Тесты синхронизатора сюжетных диалогов и валидатора PS1
│   ├── test_align_renpy_to_po.py # Тесты сопоставления с Ren'Py и валидации 216 кураторских правок
│   ├── test_patch_minigames.py   # Тесты патчера мини-игр, викторины, чармапа и шрифта SMINI (29 тестов)
│   ├── test_minigames_catalog.py # Тесты каталога мини-игр и викторины (9 тестов)
│   ├── test_analyze_combat_budgets.py # Тесты анализатора боевых бюджетов
│   └── test_*.py                 # 305 unit-тестов тулчейна (включая все подсистемы перевода)
├── build.sh                      # Единый скрипт пересборки локализации (с поддержкой --story, --minigames, --validate)
├── run_game.sh                   # Единый скрипт запуска игры в эмуляторе
└── hand_off.md                   # Этот документ передачи проекта
```

---

## 4. Памятка по командам управления

### Запуск игры в эмуляторе:
```bash
./run_game.sh              # Запуск русской версии
./run_game.sh --orig       # Запуск японского оригинала
./run_game.sh --dry-run    # Проверка файлов без открытия GUI
```

### Пересборка локализации:
```bash
./build.sh --story         # Быстрая сборка: обновить сюжетные диалоги и базовый образ (~12 сек)
./build.sh --dialogues     # Синоним --story
./build.sh --quick         # Быстрое обновление комнат (translations/room_inspection_ru.json, ~2 сек)
./build.sh --minigames     # Обновление правил 5 мини-игр и викторины из translations/minigames_ru.json
./build.sh --quiz          # Синоним --minigames
./build.sh --combat        # Обновление боевых диалогов и 2BPP-шрифта из translations/combat_dialogues_ru.json
./build.sh --services      # Обновление городских служб, меню и валюты из translations/town_services_ru.json
./build.sh --shops         # Синоним --services (диалоги лавок и городские службы)
./build.sh --cards         # Обновление карточек персонажей из translations/lore_cards_ru.json
./build.sh --map           # Обновление названий локаций на карте мира (PROG.UNT 0x001)
./build.sh --banners       # Обновление графических плашек локаций (BASYOG.UNT 466)
./build.sh                 # Полная 9-этапная сборка (~38 сек):
                           #   [1/9] Сюжетные диалоги (PROG.UNT) с авто-синхронизацией JSON
                           #   [2/9] FMV-видео с русскими субтитрами (build/movies/MOVIE.STR в LBA 127)
                           #   [3/9] Описания предметов во всех 149 комнатах
                           #   [4/9] Энциклопедические карточки персонажей (4bpp TIM)
                           #   [5/9] Названия локаций на карте мира (PROG.UNT 0x001)
                           #   [6/9] Графические плашки-баннеры локаций (BASYOG.UNT 466)
                           #   [7/9] Боевые диалоги и 2BPP-шрифт (PROG.UNT 0x007, 0x142)
                           #   [8/9] Городские службы, меню и валюта (PROG.UNT 0x003, 0x03A)
                           #   [9/9] Мини-игры и викторина (PROG.UNT 13..17, SMINI.UNT 0..4)
./build.sh --validate      # Комплексная проверка всех каталогов и дисковых секторов
```

### Управление каталогом сюжетных диалогов:
```bash
python3 tools/sync_story_dialogues.py --validate      # Аппаратная валидация всех 4 514 реплик JSON по спецификации PS1
python3 tools/sync_story_dialogues.py --sync-if-newer # Обновить dialogue.po, если JSON был изменён
python3 tools/sync_story_dialogues.py --apply         # Накатить переводы из JSON в dialogue.po
python3 tools/sync_story_dialogues.py --export        # Экспорт dialogue.po в translations/story_dialogues_ru.json
python3 tools/sync_story_dialogues.py --status        # Статистика распределения реплик по спикерам и сценам
```

### Анализ мини-игр и боевых диалогов:
```bash
python3 tools/patch_minigames.py --verify             # Быстрая верификация правил, викторины и шрифтов SMINI (EDC/ECC)
python3 tools/analyze_combat_budgets.py --check       # Проверка лимитов всех 114 блоков боя (0 overflow)
python3 tools/analyze_combat_budgets.py --repack-sim  # Симуляция динамического репакинга диалогового потока
python3 tools/analyze_combat_budgets.py --triggers    # Анализ триггерных условий боя (0x05F5F0..0x05F808)
```

### Запуск тестов:
```bash
python3 -m pytest tools/ -q                                                              # 305 тестов тулчейна
cd patch_repo && SLAYERS_ROYAL_BIN="$(realpath ../downloads/sr.bin)" python3 -m pytest localization/tests -q  # 11 тестов интеграции
```
*(Всего 316 тестов — все проходят со 100% успехом).*

---

## 5. Выполненные задачи и ключевые инженерные вехи

### 5.1. Многостраничный вывод боевых диалогов (Пагинация через опкод 0x00FD)
* **Статус:** **Выполнено на 100% (13 сентября 2026)**.
* **Контекст:** В боевом режиме диалоговое окно вмещает максимум 3 строки текста ($\le 21$ символа на строку). Ранее развёрнутые реплики из Ren'Py-скрипта приходилось сокращать под 3 строки одного баббла.
* **Реализованное техническое решение:**
  1. **Внутрибаббловая пагинация через `OPCODE_PAGE_BREAK` (`0x00FD`):**
     * Реверс-инжиниринг подтвердил, что опкод `0x00FD` выполняет внутрибаббловый переход без смены портрета: движок выводит мигающую стрелочку-индикатор, ждёт нажатия клавиши, очищает текстовую область и продолжает печать следующей страницы без перезагрузки спрайта персонажа (повторный опкод портрета `0x9...`/`0xD...` не передаётся).
     * В `tools/combat_dialogue_charmap.py` введён алиас `OPCODE_PAGE_BREAK = 0x00FD`, кодировщик переводит символ разрыва страницы `\f` (`\x0c`) в опкод `0x00FD`.
     * В `tools/patch_combat_dialogues.py` расширен энкодер `encode_conversation_block`: поддержана структура `"pages": ["стр 1", "стр 2", ...]` и `\f` в `"text_ru"`, с валидацией каждой страницы ($\le 3$ строк, $\le 21$ символ в строке).
     * Развёрнуты реплики в `translations/combat_dialogues_ru.json`:
       * Блок 106 (`0x06241C`, сцена с Ларком/Моссманом): Наёмник говорит в 2 страницы («Если б ты пошёл / с нами, эти типы» $\to$ «не стали б совать / сюда свой нос!»), полностью возвращая оригинальный смысл реплики из Ren'Py.
       * Блок 001 (`0x05F810`, сцена с Нагой): Лина говорит в 2 страницы («Нага! Учти, город / совсем рядом!» $\to$ «Драгу-Слейв / применять нельзя!»).
  2. **Анализ бюджетов и симуляция динамического репакинга (`tools/analyze_combat_budgets.py`):**
     * Проведён детальный аудит всех 114 блоков диалогов `0x05F810..0x06286A` в `PROG.UNT 0x007`.
     * Суммарный бюджет: **12 378 байт**, использовано: **11 840 байт**, свободный запас: **538 байт** (в среднем 4.72 байта на блок).
     * Переполнений: **строго 0 блоков** (все 114 блоков укладываются в оригинальные in-place границы).
     * Создан инструмент `tools/analyze_combat_budgets.py` (с флагами `--check`, `--repack-sim`, `--triggers`, `--verbose`, `--json`), подтверждающий валидность бюджетов и моделирующий глобальный репакинг с корректировкой триггеров (`0x05F5F0..0x05F808`), Table 2 (`0x05F470..0x05F504`) и Table 3 (`0x06286C..0x062A0C`).
  3. **Сохранность бинарных инвариантов и пайплайн:**
     * Системные кнопки боя `0x05F278..0x05F470` (`START`, `ATTACK`, `MOVE` и др.) и MIPS-код `0x02B78C` защищены (100% Zero UI Interference).
     * Все контрольные суммы Mode 2 Form 1 EDC/ECC пересчитаны.

### 5.2. Синхронизация чармапа мини-игр и викторины (ликвидация сдвига +1)
* **Статус:** **Выполнено на 100% (13 сентября 2026)**.
* **Проблема:** В `tools/patch_minigames.py` использовался устаревший `DEFAULT_CHARMAP` из `patch_inspection.py`, в котором кириллические символы имели ошибочный сдвиг +1 (`А` = 0x0009 вместо 0x0008, `Б` = 0x000A вместо 0x0009, `а` = 0x002A вместо 0x0029, `«` = 0x0053 вместо 0x0005 и т.д.), из-за чего в самой игре весь текст правил мини-игр и 100 вопросов викторины рендерился со сдвигом на одну букву («Тмжкжст-лгйи...» вместо «Слейерс-квиз...»).
* **Решение:**
  1. Реализована функция `build_minigames_charmap()`, строго синхронизированная с эталонным `patch_repo/localization-work/ru/build/glyph_map.json` и `PROG.UNT 0x03A`:
     * `«` = 0x0005, `»` = 0x0006
     * `Ё` = 0x0007, `А` = 0x0008, `Б` = 0x0009 ... `Я` = 0x0028
     * `а` = 0x0029, `б` = 0x002A ... `я` = 0x0050, `ё` = 0x0051
     * `—` = 0x0052, `“` = 0x0053, `„` = 0x0054, `…` = 0x0056
     * Пробел = 0x007D, Цифры `0..9` = 0x00A8..0x00B1, Пунктуация `.`, `,`, `!`, `?`, `:`, `-`.
  2. Реализована функция `get_reverse_charmap()` с приоритетом кириллических и типографических символов над устаревшими ASCII-наложениями (0x0005 $\to$ `«`, 0x0006 $\to$ `»`).
  3. Расширен диапазон копирования тайлов шрифта `MINIGAME_FONT_GLYPH_IDS` (добавлен диапазон `0x0005..0x0056`, всего 139 тайлов), обеспечивающий полную синхронизацию тайлов `SMINI.UNT` с `PROG.UNT 0x03A`.

### 5.3. Перевод UI мини-игр, заголовков-плашек и экранов викторины
* **Статус:** **Выполнено на 100% (13 сентября 2026)**.
* **Проблема:** В английской базе `sr_patched.bin` заголовки окон мини-игр, экран выбора участника викторины и экран результатов были пропатчены 1-based ASCII-индексами (`A=1..Z=26`), что при внедрении русского шрифта вызывало моджибейк (абракадабру) на 4 экранах: `Л Д ( Р « К Л   Й Н Б С` (Quiz Title), `И Б / Г   И Д ( Р « К` (Pick Player), `Л / Э К «` (Score), `) ( Ж ~ Б М   И Д ( М « Отстрел` (Bandit Bullying). Кроме того, `rules_ru` дублировали названия игр, выводя заголовок дважды.
* **Решение:**
  1. **Заголовки-плашки всех 5 мини-игр:** Entry 13 (0x07320): «Обед:», Entry 14 (0x11318): «Прыжок:», Entry 15 (0x07EB4): «Смех Наги:», Entry 16 (0x08186): «Викторина:», Entry 17 (0x082B8): «Отстрел:».
  2. **Очистка текстов правил (`rules_ru`):** Во всех 5 мини-играх убран дублирующий заголовок, текст начинается непосредственно с правил и инструкций геймплея.
  3. **Интерфейс викторины (Entry 16):** Экран выбора участника (0x08284): «Кто играет?», Экран результатов — заголовок (0x0829C): «Итог:», верные ответы (0x082AC): «Верно:     в», среднее время (0x082C6): «Ср. время:     с».
  4. **Zero-fill policy:** Перед записью каждой строки буфер полностью зануляется `0x00`, исключая любые паразитные байты.

### 5.4. Синхронизация мини-игр с Ren'Py и полная ликвидация моджибейка всех 5 мини-игр
* **Статус:** **Выполнено на 100% (14 сентября 2026)**.
* **Реализованные задачи:**
  1. **Выравнивание вопросов и ответов викторины с `quiz.rpy`:**
     * Все 100 вопросов сопоставлены с Ren'Py v0.9.9.
     * 18 вариантов ответов и 5 формулировок вопросов отшлифованы и выровнены для устранения искажений перевода.
     * Проверены индексы правильных ответов (`correct: 0..2`) на 100% соответствие ответам Ren'Py.
  2. **Смех Наги (Entry 15): перевод всех 6 диалогов:**
     * Полностью переведены реплики Наги перед состязанием, реакция на наряд, на долг, на толчок и победная фраза (`0x07FC8..0x0833A`).
     * Устранены мусорные байты `У О Ь И П...`.
     * Внедрён опкод смены страниц `0x00FE` (`\f`).
     * Сохранена 5-указательная таблица RAM `NAGA_DIALOGUE_POINTERS` (`0x08308..0x0831C`), переведён статус «Нага довольна!».
  3. **Прыжок Амелии (Entry 14): перевод пафосных речей Справедливости:**
     * Переведены 5 речей Справедливости (`0x1147C..0x11850`) и финальная строка `justice_up` («Справедливость!»).
     * Ликвидирован артефакт японского кандзи `生` (`0x0163`).
     * Сохранена таблица 5 RAM-указателей `AMELIA_DIALOGUE_POINTERS` (`0x11854..0x11868`).
  4. **Вертикальное окно комбинаций поз Амелии:**
     * Восстановлены оригинальные иконки кнопок геймпада (`○`, `↑`, `↓`, `→`, `←` на `0x006B`, `0x0074..0x0077`) в TIM-шрифте Entry 1 пакета `SMINI.UNT`.
     * Защищён диапазон контроллерных глифов `SMINI_PROTECTED_GLYPHS` (`0x0057..0x0076`), предотвращая перезапись иконок кириллицей.
     * Переведены потоки комбинаций (`ОК:`, `УРА:`, `УРБ:`, `УРВ:`, `УРС:`) для 3 уровней сложности (`0x11868`, `0x118C4`, `0x11928`).
     * Сохранена 3-указательная таблица `AMELIA_COMBO_POINTERS` (`0x1198C..0x11998`).
     * Полностью устранены кракозябры `э г ь / Д О ( ь / У г...`.
  5. **Обед (Entry 13) и Отстрел бандитов (Entry 17):**
     * Переведены 3 внутриигровые статусные реплики Лины и Гаури в таверне (`lina_morale`, `gourry_satiated`, `combined_status`).
     * Переведена строка бонуса времени в тире (`time_bonus`: «Время!»).

### 5.5. Создание мастер-каталога сюжетных диалогов и конвейера сборки
* **Статус:** **Выполнено на 100% (14 сентября 2026)**.
* **Реализованные задачи:**
  1. **Мастер-каталог `translations/story_dialogues_ru.json`:**
     * Полный структурированный каталог всех **4 514 сюжетных диалогов** в 30 сценах игры.
     * Содержит идентификаторы сцен, названия глав, русских спикеров (`SPEAKERS_GLOSSARY`), японский оригинал и редактируемый русский текст.
  2. **Инструмент синхронизации `tools/sync_story_dialogues.py`:**
     * Двусторонняя конвертация PO $\leftrightarrow$ JSON.
     * Строгая аппаратная валидация лимитов PS1 ($\le 15$ симв./стр, 1..3 стр., Unicode NFC, запрет `\r`, проверка `\f` для FF-сегментов).
     * Флаги CLI: `--export`, `--apply`, `--sync-if-newer`, `--validate`, `--status`.
  3. **Оптимизация сборочного скрипта `build.sh`:**
     * Автоматическая синхронизация диалогов при сборке (`--sync-if-newer`).
     * Режим быстрой сборки `./build.sh --story` (~12 секунд) для мгновенной проверки изменений текста в игре.
     * Интеграция валидации в `./build.sh --validate`.
  4. **Сводный реестр `translations/translations_index.json`:**
     * Индексация всех 8 файлов локализации проекта в папке `translations/`.

### 5.6. Устранение чёрного экрана в Сонии и краша в Отеле (коллизия буферов 0x03E и 0x058)
* **Статус:** **Выполнено на 100% (14 сентября 2026)**.
* **Контекст и симптомы:** При входе в город Сония игра зависала с чёрным экраном, либо при попытке ночлега в отеле Сонии эмулятор падал с фатальной ошибкой памяти.
* **Реверс-инжиниринг коллизии памяти в RAM:**
  * Буфер загрузки текущей сюжетной сцены начинается с адреса **`0x80121000`** (максимальный размер 44 384 байта).
  * Контейнер скриптов и триггеров комнат `PROG.UNT 0x058` жёстко привязан к аппаратному адресу в RAM:
    $$\mathbf{0x8012A800}$$
  * Доступный физический бюджет для сцены Сонии `0x03E`:
    $$0x8012A800 - 0x80121000 = 0x9800 = 38\,912 \text{ байт} = \mathbf{19 \text{ секторов}}$$
  * В старой сборке сцена `0x03E` была ошибочно расширена на +13 секторов (до 32 секторов = 65 536 байт). При загрузке `0x03E` занимала диапазон `0x80121000..0x80131000`.
  * Последующая загрузка блока `0x058` по адресу `0x8012A800` физически перезаписывала хвостовую область `0x03E`, где хранится таблица указателей (`pointers`).
  * При обращении к кью отеля `0x04A6` движок пытался прочитать указатель `0x00C5` в разрушенной таблице `0x03E`, считывал мусорные байты `0x7FF04F6D` / `0x7FEEC283` и падал по `Address Error Exception`.
* **Реализованное исправление:**
  1. **Сцена `0x03E` строго ограничена 19 секторами (38 912 байт):** В `patch_repo/localization/script.py` расширение для `0x03E` исключено (`EXTRA_EXPANSION_SECTORS`), перекрытие с `0x058` сведено к **строго 0 байт**.
  2. **Оптимизация текста до 37 848 байт:** Утилитой `tools/fit_dialogue_budget.py` русский текст сцены Сонии оптимизирован до 37 848 байт, обеспечив запас в **1 064 байта** свободной памяти внутри 19 секторов.
  3. **Сохранность кураторских правок:** Все **216 записей `POLISHED_OVERRIDES`** в сцене Сонии сохранены на 100%.
  4. **Исправление наложения буфера `status_just_ate`:** В `tools/patch_town_services.py` исправлен буфер статусной реплики «Мы только поели», устранив наложение на таблицу переходов таверны `0x02D4CC`.
  5. **Синхронизация сейвстейта эмулятора:** В файле сохранения `SLPS-01363_6.sav` дамп RAM в сейвстейте эмулятора синхронизирован с новыми адресами и валидными указателями.

### 5.7. Ликвидация моджибейка в лавках («ЩФР ПХИПР?») и создание каталога shop_dialogues_ru.json
* **Статус:** **Выполнено на 100% (14 сентября 2026)**.
* **Контекст и симптомы:** При входе в оружейную лавку приветствие торговца отображалось в виде нечитаемых символов «ЩФР ПХИПР?» вместо «Что нужно?». В других магазинах реплики торговцев и торга персонажей либо выводились с искажениями, либо оставались непереведёнными.
* **Реверс-инжиниринг причины бага (Root Cause):**
  * В `tools/patch_town_services.py` таблица символов `DEFAULT_CHARMAP` импортировалась из `tools/patch_inspection.py`, где кириллические глифы были смещены на 1 код относительно VRAM-шрифта (`Ч` = 0x0021 вместо 0x0020, `т` = 0x003E вместо 0x003D, `о` = 0x0039 вместо 0x0038, `н` = 0x0038 вместо 0x0037, `у` = 0x003F вместо 0x003E и т.д.).
  * В то же время растровый шрифт, загружаемый в видеопамять VRAM из `PROG.UNT 0x03A`, скомпилирован по спецификации `glyph_map.json` / `AUTHORITATIVE_CYRILLIC_CHARMAP`, где код 0x0021 закреплён за буквой `Щ`, 0x001C — за `Ф`, 0x0018 — за `Р`.
  * Из-за этой рассинхронизации строка `<D9A1>Что нужно?` кодировалась байтами `0x0021, 0x003E, 0x0039...` и отображалась как «ЩФР ПХИПР?».
* **Реализованное решение:**
  1. **Синхронизация чармапа:** Таблица символов в конвейере патчинга лавок строго синхронизирована с эталонным шрифтом `PROG.UNT 0x03A` (`patch_repo/localization-work/ru/build/glyph_map.json` и `tools/patch_minigames.py:AUTHORITATIVE_CYRILLIC_CHARMAP`).
  2. **Выделенный каталог `translations/shop_dialogues_ru.json`:**
     * Каталог содержит все **98 уникальных текстовых слотов**, полностью покрывающих **100 указателей** в `PROG.UNT 0x003` (84 указателя Table 1 + 16 указателей Table 2).
  3. **Перевод таблицы наименований товаров и параметров (`shop_items`, Таблица 0x030A20):**
     * В каталог добавлен раздел `shop_items` со всеми **91 наименованиями** (указатели `0x030A20..0x030B88`, строки `0x03054C..0x030A10`): имена персонажей (Лина, Нага, Гаури, Зелгадис, Амелия, Сильфиль, Ларк), названия характеристик, оружия, брони, аксессуаров и сюжетных предметов.
     * **Ликвидация коллизии в окне статов и залипания артефактов на экране:** Ширина колонок красного окна сравнения параметров экипировки составляет всего 6–7 знакомест. Полные названия (`Физ. атака`, `Уклонение` и т.д.) перекрывали цифры параметров и вылезали за правую границу окна, оставляя неудаляемый графический артефакт `ть Уклонени` позади портрета торговца. Параметры 07..12 переведены в строгий 5-символьный формат: `Ф.Атк`, `Ф.Защ`, `М.Атк`, `М.Защ`, `Точн.`, `Уклон`.
  4. **Устранение бага наложения оверлея оружия (Hardware Overlay Token `<00BF>`):**
     * **Причина:** Токен `<00BF>` рендерится движком аппаратным оверлеем по жёстко фиксированным экранным координатам (Line 2 $Y = 56$). В `buy_suggest_weapon` наличие текста перед токеном приводило к отрисовке названия поверх букв `то есть `. В `buy_material_weapon` второй токен рендерился на Line 2 поверх строки `Есть отличная вещь`.
     * **Решение:** Во всех диалогах с токеном предмета `<00BF>` он вынесен строго в начало строки:
       * `buy_suggest_weapon`: `Есть получше:\n<00BF>.\nКак тебе?` (Line 1: 13 симв., Line 2: оверлей, Line 3: 9 симв.)
       * `buy_material_weapon`: `<00BF>?\n<00BF>\nв наличии есть.` (Line 1: 7 симв., Line 2: оверлей, Line 3: 15 симв.)
       * `buy_out_of_stock_weapon`: `<00BF> у нас\nне водится.\nУвы!`
       * `buy_suggest_item`: `<00BF>? Как насчёт\n<00BF>? Похоже,\nно куда лучше!`
       * `buy_material_item`: `<00BF> нужен?\nЕсли получше,\nкак вот это?\f<00BF> — материал,\nтак что прослужит\nкуда надёжнее!`
  5. **Сохранение системных переменных:** Корректно сохранены переменные движка `<00BF>` (название предмета, параметр D) и `<00BE>` (число золотых монет, параметр A).
  6. **Аппаратные ограничения PS1:** Все реплики строго проверены на лимит $\le 15$ символов в строке ($15 \times 16 = 240$px), 1..3 строки в окне, перенос строк `\n`, разрыв страницы `\f`.
  7. **Динамический пул свободной памяти (0x0327E0..0x094000):** Длинные русские реплики, превышающие размер оригинальных слотов, автоматически переносятся в свободное пространство Entry 3 с пересчётом 32-битных указателей.
  8. **Тестирование и комплексная валидация:**
     * Создан юнит-тест `tools/test_shop_catalog.py` (**6 тестов**), проверяющий структуру метаданных, полное покрытие 100 указателей диалогов, лимиты текста, чармап-валидность, таблицу 91 товара, ширину колонок статов ($\le 5$ символов) и позиционирование `<00BF>` в начале строк.
     * Каталог зарегистрирован в `translations/translations_index.json` (общее число каталогов: **9**).
     * Обновлён тест `tools/test_sync_story_dialogues.py` (**18 тестов**), подтверждена валидность реестра.
     * Тестовый набор `tools/test_patch_town_services.py` (**16 тестов**) подтверждает патчинг 191 указателя (91 предмет + 84 buy + 16 sell), позиционирование оверлея и расчёт контрольных сумм EDC/ECC (всего **40 тестов** в тулчейне лавок).
     * Инструментом `tools/sync_savestates.py` синхронизированы все 7 сейвстейтов эмулятора (`SLPS-01363_*.sav`), гарантируя отсутствие моджибейка как в новой игре, так и при загрузке сохранений.

---

### 5.8. Ликвидация моджибейка на главной карте («ХСЙДСБнт», «ДСБНТУок») и полный перевод сектора 1606 (PROG.UNT Entry 1)
* **Статус:** **Выполнено на 100% (16 сентября 2026)**.
* **Контекст и симптомы:**
  1. На глобальной карте мира в золотой ленте вместо «ФРИГРАНТ» выводились символы «ХСЙДСБнт», а в нижней табличке вместо «ГРАМСТОК» — «ДСБНТУок».
  2. Под названием локации в нижней табличке выводился неудалённый английский текст «BACK RD» или «SONIA HWY».
* **Реверс-инжиниринг причины бага (Root Cause):**
  1. В `tools/patch_world_map.py` таблица `DEFAULT_CHARMAP` ошибочно импортировалась из `tools/patch_inspection.py`, где кириллические глифы имели сдвиг $+1$ (`А` = 0x0009 вместо 0x0008, `Ф` = 0x001E вместо 0x001D, `Р` = 0x001A вместо 0x0019, `И` = 0x0011 вместо 0x0010, `Г` = 0x000C вместо 0x000B). В видеопамяти VRAM (`PROG.UNT 0x03A`) глифы 0x001E, 0x001A, 0x0011 соответствуют буквам 'Х', 'С', 'Й'. В результате «ФРИГРАНТ» рендерился как «ХСЙДСБнт», а «ГРАМСТОК» — как «ДСБНТУок».
  2. В секторе 1606 (`LBA 230626`) `PROG.UNT` слоты городов были перепутаны: в слот 2 был записан Фригрант (вместо Грамстока), в слот 3 — Грамсток (вместо Сонии), в слот 4 — Сейрун (вместо Марк-Уэллса). Вторая таблица городов (`0x6C4..0x783`) не обновлялась вовсе.
  3. В диапазоне `0x0204..0x0380` сектора 1606 оставались английские строки из мода gourry-hacks (`SONIA HWY`, `SONIA [0x28F] RD`, `BARK W FOR`, `BORDER`, `WEST`).
* **Реализованное решение:**
  1. В `tools/patch_world_map.py` подключён каноничный чармап `build_authoritative_vram_charmap()`, синхронизированный с `PROG.UNT 0x03A`.
  2. Полностью переписана функция `patch_world_map_sector_1606`:
     * Заполнены все 12 слотов Таблицы 1 (`0x150..0x20F`) и Таблицы 2 (`0x6C4..0x783`): *ЛЕЙКВУД, БАРКЛЕНД (с лигатурой НД 0x028A), ГРАМСТОК (с лигатурой ОК 0x028D), СОНИЯ, МАРК-УЭЛЛС (0x02C7), ИЗЕЛЬС., ФРИГРАНТ (с лигатурой НТ 0x028C), КЬЮЗАК, СЕЙРУН, САНБУРГ, ТУР-СИТИ (0x02B6), ЛЕЗАРИАМ (0x02D3)*.
     * Заполнены все 21 слот дорог, лесов и границ (`0x0204..0x0380`): `ВОСТ.БАРК`, `ЗАП. БАРК`, `ВОСТ.ГРАМ`, `ШОССЕ СОН`, `ТРОПА СОНИИ`, `ЮЖН. БАРК`, `СЕВ.ИЗЕЛЬ`, `ГР.СЕЙРУН`, `З.КЬЮЗ.`, `СЕВ.ФРИГР.`, `ЮГ СЕЙР`, `С.КЬЮЗ.`, `В.СЕЙР.`, `ГР.РАЛЬТИГ`, `В.САНБ.`, `С.ТУР-СИТ`, `РЕГИОН`, `ДОРОГ`.
  3. Пересчитаны Mode 2 Form 1 EDC/ECC для секторов 89, 93 и 1606.

---

### 5.9. Устранение артефакта «表通 a» и английской вывески «MAIN» в графических плашках локаций (BASYOG.UNT 466)
* **Статус:** **Выполнено на 100% (16 сентября 2026)**.
* **Контекст и симптомы:**
  1. При переходе на локацию улицы выводилась японская надпись «表通» с серым артефактом «a» справа, либо английская вывеска «MAIN».
* **Реверс-инжиниринг причины бага (Root Cause):**
  1. В `tools/patch_location_banners.py` прямоугольник очистки `clear_bbox` для плашки `MAIN ST` был ограничен координатами `(0, 0, 85, 24)`. В оригинальной японской текстуре иероглиф `り` в надписи `表通り` продолжался до $X = 95$. Очистка до 85 пикселей оставляла правый хвост иероглифа на $X = 85..95$, который выглядел как артефакт `'a'`.
  2. При сборке через `localize.py build` базовый образ пересоздавался из `sr_patched.bin`, где вывеска содержала английское слово `MAIN`.
* **Реализованное решение:**
  1. В `tools/patch_location_banners.py:render_cyrillic_banners()` внедрена принудительная очистка всех 9 полос текстового слоя (`Y = 0..216, X = 0..256`) в `(0, 0, 0, 0)`.
  2. Границы спрайта `MAIN ST` расширены до `(0, 0, 95, 24)`, а `PLAZA` — от `(96, 0, 165, 24)`.
  3. В `translations/location_banners_ru.json` вывеска зафиксирована как чистая надпись **«ГЛАВНАЯ»** с центрированием по синей плашке.
  4. Сжатый TIM записан в `BASYOG.UNT Entry 466` (LBA 240133..240140); превью `data/preview_basyog_466_ru.png` подтверждает 0% мусорных пикселей.

---

### 5.10. Устранение зависания оружейника Сонии и аппаратного переполнения GPU-пакета координат (,3970э397|||)
* **Статус:** **Выполнено на 100% (16 сентября 2026)**.
* **Контекст и симптомы:** В оружейной лавке Сонии при выборе Меча Света продавец выдавал строку «Хм, лучше этого», а на второй строке появлялся числовой мусор `,3970э397||| э3996 393`. Окно зависало и оставалось на экране до выхода из лавки.
* **Реверс-инжиниринг причины бага (Root Cause):**
  1. Реплика `buy_none_better_weapon` содержала 15 символов текста + 1 слово спикер-кода `<D9A1>` = **16 слов** в первой строке до `0x00FE`.
  2. Аппаратный лимит строкового буфера PS1 для окон магазинов составляет строго $\le 15$ слов.
  3. При 16 словах происходило переполнение буфера GPU-пакета координат в RAM: координаты следующего графического примитива интерпретировались отрисовщиком как глифы и рендерились поверх строки 2 в виде числового мусора. Движок зацикливался в цепочке Ordering Table (OT) и зависал.
  4. Анализ выявил ещё 6 реплик с аналогичным переполнением (`buy_greet_magic`, `haggle_ask_magic`, `haggle_counter_weapon_1`, `haggle_offer_lina`, `sell_greet_magic`, `sell_done_magic`).
* **Реализованное решение:**
  1. Все 7 реплик сокращены до $\le 14$ символов со спикер-кодом ($\le 15$ слов суммарно):
     * `buy_none_better_weapon`: **«Хм, получше?\nУ меня такого\nв лавке нет.»** (строка 1: 12 символов + спикер = 13 слов $\le 15$).
     * `buy_greet_magic`: **«Что угодно?»** (12 слов $\le 15$).
     * `haggle_ask_magic`: **«Какую цену\nвы готовы\nпредложить?»** (11 слов $\le 15$).
     * `haggle_counter_weapon_1`: **«Это грабёж!\nВот золотых:\n<00BE> — пойдёт?»** (12 слов $\le 15$).
     * `haggle_offer_lina`: **«Как вам цена\nв <00BE> золотых?»** (13 слов $\le 15$).
     * `sell_greet_magic`: **«Что продаёте?»** (14 слов $\le 15$).
     * `sell_done_magic`: **«Вот оплата.\nЧто-то ещё\nпродадите?»** (12 слов $\le 15$).
  2. Обновлены `translations/shop_dialogues_ru.json` и `tools/build_shop_catalog.py`.
  3. Пропатчен `PROG.UNT Entry 3`, синхронизированы все 7 сейвстейтов эмулятора DuckStation (`SLPS-01363_*.sav`). Диалог завершается плавно и без зависаний.

---

### 5.11. Однородность шрифта диалогов (100% каноничный 11pt кегль) и полный аудит синхронизации с Ren'Py Edition
* **Статус:** **Выполнено на 100% (16 сентября 2026)**.
* **Контекст и симптомы:**
  1. В диалогах часть букв отображалась стандартным крупным кеглем $11\text{ pt}$, а часть — мелкими сплюснутыми парами (DTE-токены), создавая эффект «пляшущего» разношёрстного шрифта.
* **Реверс-инжиниринг причины бага (Root Cause):**
  1. Параметр `"compact_glyph_limit": 192` в `language.json` приводил к генерации 192 двухбуквенных тайлов мелким шрифтом ($6\text{–}7\text{ pt}$), которые подставлялись токенизатором во ВСЕ сюжетные сцены игры (`SCENE_ENTRIES`), включая вступительную сцену в Лейквуде (`0x03B`).
* **Реализованное решение:**
  1. В `patch_repo/localization-work/ru/language.json` параметр сброшен в `"compact_glyph_limit": 0`.
  2. Все сжатые двухбуквенные токены полностью исключены. **Шрифт во всех 30 сценах игры стал на 100% однородным, монолитным и полноразмерным ($11\text{ pt}$ Press Start 2P)**.
  3. **Аудит синхронизации с Ren'Py Edition по всем 30 сценам:**
     * В **29 из 30 сцен** игры (всего **3 698 диалогов**) перевод перенесён **слово в слово из Ren'py Edition (100.0% совпадение)**:
       * 0x03B (319/319, 100%), 0x03C (398/398, 100%), 0x03D (341/341, 100%), 0x03F (59/59, 100%), 0x040 (483/483, 100%), 0x041 (342/342, 100%), 0x042 (247/247, 100%), 0x043 (228/228, 100%), 0x044 (145/145, 100%), 0x045 (160/160, 100%), 0x046 (123/123, 100%), 0x047 (5/5, 100%), 0x048 (60/60, 100%), 0x049 (26/26, 100%), 0x04A (44/44, 100%), 0x04B (14/14, 100%), 0x04C (65/65, 100%), 0x04D (27/27, 100%), 0x04E (21/21, 100%), 0x04F (156/156, 100%), 0x050..0x056 (140/140, 100%), 0x053 (180/180, 100%), 0x055 (45/45, 100%), 0x057 (58/58, 100%), 0x139 (12/12, 100%).
     * В сцене Сонии (`0x03E`, 816 диалогов): 482 реплики перенесены 1 в 1, а 334 реплики компактно адаптированы с сохранением полного смысла, юмора и характера героев Ren'py (без многоточий и без обрывов предложений). Это обеспечивает строгое соблюдение лимита $19$ секторов ($38\,912$ байт), нулевое перекрытие с блоком $0x058$ в RAM и $100\%$ однородный шрифт.
  4. Исправлен скрытый баг с катаканой в сцене `0x04F`: `В Фриграント` $\to$ `Во Фригрант`.

---

### 5.12. Внедрение русских субтитров во все 12 видеороликов MOVIE.STR
* **Статус:** **Выполнено на 100% (16 сентября 2026)**.
* **Реализованная задача:**
  1. В `build/movies/` содержатся все 12 видеороликов (`s00.str`..`s11.str`, 433 МБ) с захардсабленными русскими субтитрами из Ren'py (`s01_ru.srt`..`s11_ru.srt`).
  2. Утилитой `tools/fmv_pipeline.py` видеопоток внедрён в LBA 127 диска `slayers_royal_ru.bin` с сохранением интерлива 1/32 и физических заголовков BCD MSF. Проверка сектора 127 подтверждает 100% совпадение с субтитрированным потоком.

---

### 5.13. Полная русификация названий всех комнат и локаций игры (PROG.UNT 0x059..0x0F8) и ликвидация артефактов «BAR», «表通 a», «宿屋», «BACK»
* **Статус:** **Выполнено на 100% (16 сентября 2026)**.
* **Контекст и симптомы:**
  * В игре при исследовании локаций в верхней навигационной плашке отображались артефакты:
    1. «BAR» (английское название заведения `定食屋`).
    2. «表通 a» (японские кандзи `表通` с английской буквой `a` из-за подстановки глифа `0x0028` вместо `り` в `表通り`).
    3. «宿屋» (японские кандзи гостиницы `0x159 0x10f`).
    4. «BACK» (английское название закоулков `裏通り`).
    5. И все остальные локации оставались либо на английском («PLAZA», «CHURCH», «WOODS», «VAULT», «STORE», «SEWER» и др.), либо на японском.
* **Реверс-инжиниринг причины бага (Root Cause):**
  * В оригинальном движке и английском патче названия комнат хранятся в заголовках записей `PROG.UNT 0x059..0x0F8` по смещению `HDR_ROOM_NAME_PTR = 0x003C` (база RAM `0x00200000`).
  * Для 37 комнат, восстановленных из японского оригинала `downloads/sr.bin`, названия были скопированы в виде сырых японских байтов (`0xeb 0xec 0x28` `表通り`, `0x159 0x10f` `宿屋`). В результате замены шрифта в VRAM `0x0028` рендерился как буква `a` (получалось «表通 a»), а кандзи `宿屋` оставались непереведёнными.
  * Для остальных 123 комнат названия оставались на английском языке из английского фанатского патча.
  * Инжектор `tools/patch_inspection.py` содержал жёсткое ограничение `rn_offset + len(rn_enc) <= next_hdr`, из-за которого русские названия, превышающие размер исходного слота (например, «ОТЕЛЬ» 12 байт > 6 байт), молча игнорировались и не записывались в игру.
* **Реализованное решение:**
  1. **Создан 10-й мастер-каталог `translations/room_names_ru.json`:**
     * Содержит все **160 комнат игры (`0x059..0x0F8`)** и полный словарь всех **48 уникальных локаций** на русском языке.
     * **Строгое аппаратное ограничение ширины плашки ($\le 10$ символов):** в движке PS1 шрифт комнат рендерится фиксированной сеткой 16×16 px/символ (`x += 16`), а внутренняя ширина бокса вывески ограничена **160 px**. При длине $> 10$ символов движок отсекает текст по правому краю (`З. ЛЕС БАР...`). Все 48 названий каталога оптимизированы до $\le 10$ символов:
       * «БАР» (`0x05D`), «ГЛАВНАЯ» (`0x059`), «ОТЕЛЬ» (`0x05C`), «ЗАКОУЛКИ» (`0x05A`).
       * Леса Баркленда: «З.БАРКЛЕНД» (`0x0D9`, 10 симв.), «В.БАРКЛЕНД» (`0x0D8`, 10 симв.), «Ю.БАРКЛЕНД» (`0x0DE`, 10 симв.) — слово «БАРКЛЕНД» отображается на 100% целиком и без обрезки!
       * «ПЛОЩАДЬ», «ЦЕРКОВЬ», «ОРУЖЕЙНАЯ», «ЛАВКА», «ТАВЕРНА», «РЫНОК», «ОКРАИНА», «СТАРОСТА», «ДОМ МЭРА», «ДОМ ЛАРКА», «ГИЛЬДИЯ», «БИБЛИОТЕКА», «ХРАМ», «СКЛАД», «ОСОБНЯК», «КОЛЛЕКТОР», «ЗАЛ», «СОКРОВИЩА», «ПЕЩЕРА», «В ПЕЩЕРЕ», «ПОБЕРЕЖЬЕ», «ПРИСТАНЬ», «КОРИДОР», «ЛЕС», «С.ИЗЕЛЬСЕН», «ГР.РАЛЬТИГ», «ГР.СЕЙРУН», «ТРАКТ», «ОБХОД», «С. КЬЮЗАК», «З. КЬЮЗАК», «В.ГРАМСТОК», «В. САМБУРГ», «В. СЕЙРУН», «С.ТУР-СИТИ» и др.
  2. **Реализована динамическая релокация длинных названий в `tools/patch_inspection.py`:**
     * Если русское название помещается в заголовочный слот (`<= next_hdr`), оно записывается in-place с занулением остатка.
     * Если русское название длиннее исходного слота, строка размещается в свободном пространстве после trailing block (`new_rn_offset = (new_data_end + 3) & ~3`), а 32-битный указатель `HDR_ROOM_NAME_PTR` по смещению `0x003C` автоматически обновляется на `RAM_BASE + new_rn_offset`.
     * Механизм `progressive_shorten` автоматически резервирует бюджет под релоцированное название, гарантируя строго 0 переполнений секторов во всех 160 записях.
  3. **Прямая поддержка 11 неинтерактивных пещерных комнат (`0x06C..0x076`):**
     * Комнаты пещеры без объектов инспекции теперь корректно получают русское название «В ПЕЩЕРЕ» (18 байт ровно в доступный слот `0x42..0x54`).
  4. **Устранение усечения плашки «ТАВЕРНА» в `BASYOG.UNT 466`:**
     * В `tools/patch_location_banners.py` и `translations/location_banners_ru.json` скорректированы границы прямоугольников очистки `TAVERN` и `TEMPLE`, благодаря чему надпись «ТАВЕРНА» отображается полностью и без усечения буквы «А».
  5. **Синхронизация сейвстейтов эмулятора DuckStation (`tools/sync_savestates.py`):**
     * В `tools/sync_savestates.py` добавлена функция `sync_room_names_in_payload`, которая находит в памяти RAM активную комнату и обновляет её название на русское во всех 8 сейвстейтах `SLPS-01363_*.sav`.
  6. **Верификация:**
     * Все 160 записей `PROG.UNT 0x059..0x0F8` на диске `slayers_royal_ru.bin` проверены чтением и декодированием (160/160 PASSED, 100% совпадение с каталогом).
     * Добавлен специализированный набор тестов `tools/test_room_names.py` (5 passed). Все 332 теста проекта зелёные.

---

### 5.14. Устранение вылета в странной гостинице Изельсена (0x040 vs 0x058 at 0x8012A800) и динамический LBA для Entry 0x03A
* **Статус:** **Выполнено на 100% (16 сентября 2026)**.
* **Контекст и симптомы:**
  * При входе в странную гостиницу города Изельсен (`0x095`..`0x09A`, сцена `0x040`) игра зависала с массовым спамом в лог:
    `[ 1425.4286] E(UnknownReadHandler): Invalid halfword read at address 0x7FEF88F8, pc 0x80075D38`
* **Реверс-инжиниринг причины бага (Root Cause):**
  1. В ассемблерном коде `PROG.UNT Entry 3` по адресу `0x80075D38` вызывается обработчик гостиничного кью `0x060E` сцены `0x040`: движок загружает базу таблицы указателей из `0x80121000` (куда распаковывается текущая сюжетная сцена) и читает 32-битный указатель на диалог.
  2. В `patch_repo/localization/script.py` сцена `0x040` (город Изельсен) была расширена до **20 секторов (40 960 байт)**.
  3. Системный модуль комнат `PROG.UNT 0x058` загружается в RAM по строго захардкоженному аппаратному адресу **`0x8012A800`** (`38 912 байт` от базы сцены `0x80121000`, строго 19 секторов).
  4. При входе в гостиницу модуль `0x058` загружался по адресу `0x8012A800` и **намертво затирал хвост сцены 0x040** (`0x8012A800..0x8012B000`), где располагалась таблица указателей реплик (смещение `0x9BAC`).
  5. Вместо валидного указателя игра считывала машинный код `0xF8780000` из `0x058`, прибавляла базу `0x7FEF1000`, получала мусорный адрес `0x7FEF88F8` и бесконечно выбрасывала `UnknownReadHandler Exception`.
* **Реализованное решение:**
  1. **Строгое закрепление 19 секторов для сцены 0x040:** в `patch_repo/localization/script.py` параметр `EXTRA_EXPANSION_SECTORS[0x040]` уменьшен с 5 до 4 (размер сцены стал строго **19 секторов = 38 912 байт**). Перекрытие с блоком `0x058` равно **строго 0 байт**.
  2. **Оптимизация диалогов Изельсена:** утилитой `tools/fit_dialogue_budget.py` диалоги сцены `0x040` оптимизированы (40 многостраничных реплик компактно ограничены 2 страницами). Текст полностью уложился в 38 912 байт с запасом 772 байта до границы `0x058` (`0x8012A4FC` vs `0x8012A800`).
  3. **Динамический LBA для Entry 0x03A:** в `tools/patch_town_services.py` устранено захардкоженное смещение сектора `3074`, добавлена функция `get_entry_03a_location()`, считывающая истинное положение Entry 58 из оглавления TOC `PROG.UNT`.
  4. **Синхронизация и сборка:** сюжет и образ пересобраны (`./build.sh --story`), все 332 теста проекта зелёные. Вылет в странной гостинице Изельсена ликвидирован на 100%.
